# 玉露智检 — 项目启动手册

**适用对象：** 答辩评委、导师、后续维护人员  
**版本：** v1.0  
**日期：** 2026-09-07  

---

## 一、项目简介

玉露智检是基于计算机视觉的恩施玉露茶品质分级与溯源平台。系统以 YOLOv8 检测模型为核心，对茶叶图像进行等级识别（特级 / 一级 / 二级 / 等外），并通过哈希链记录检测过程，实现不可篡改的溯源验证。

---

## 二、环境要求

| 组件 | 要求 |
|---|---|
| 操作系统 | Windows 10/11、macOS、Linux |
| Python | 3.10 - 3.13（推荐 3.13） |
| Node.js | 18 LTS 或更高（推荐 22 LTS） |
| 浏览器 | Chrome / Edge / Firefox 最新版 |
| 显卡 | 无强制要求，CPU 推理即可 |

---

## 三、目录结构

```
毕业设计/
├── src/
│   ├── backend/          # FastAPI 后端
│   │   ├── main.py
│   │   ├── auth.py
│   │   ├── detections.py
│   │   ├── traceability.py
│   │   ├── database.py
│   │   └── ui_schema.json
│   ├── inference/        # YOLOv8 推理封装
│   │   └── detector.py
│   └── hashchain/        # 哈希链实现
│       └── chain.py
├── frontend/             # Vue 3 + Vite 前端
│   ├── src/
│   │   ├── pages/        # 9 个页面
│   │   ├── components/   # SchemaRender、ECharts 组件
│   │   ├── api/          # 接口封装
│   │   └── stores/       # Pinia 状态
│   ├── package.json
│   └── tests/e2e/        # E2E 测试
├── models/               # 模型权重（需手动放置）
│   ├── best_detect.pt    # 检测主模型（YOLOv8n）
│   ├── best_cls.pt       # 分类对照模型
│   ├── training_curves.png
│   └── test_metrics.json
├── scripts/
│   └── start_backend.py  # 后端一键启动
├── tests/
│   └── test_backend.py   # 后端接口测试
├── requirements-backend.txt
└── README.md
```

---

## 四、启动步骤

### 4.1 获取代码

```bash
# 方式一：GitHub（需网络通畅）
git clone https://github.com/c8shakei/tea-yulu-grading-platform.git

# 方式二：离线 zip 包
cd tea-yulu-grading-platform
```

### 4.2 放置模型权重

将以下文件放入 `models/` 目录：

- `best_detect.pt`：YOLOv8n 检测主模型
- `best_cls.pt`：YOLOv8n-cls 分类对照模型
- `training_curves.png`：W2 训练曲线图
- `test_metrics.json`：W2 测试指标 JSON

> 模型文件体积较大，未提交 Git。首次部署时必须手动放置或运行准备脚本。

### 4.3 启动后端

```bash
# 创建并激活虚拟环境
python -m venv .venv
.venv\Scripts\activate        # Windows
# source .venv/bin/activate   # macOS/Linux

# 安装依赖
pip install -r requirements-backend.txt

# 启动服务
python scripts/start_backend.py
```

启动成功后，控制台显示：

```
Uvicorn running on http://0.0.0.0:8000
```

### 4.4 启动前端

另开一个终端：

```bash
cd frontend

# 安装依赖
npm install

# 开发模式启动
npm run dev
```

启动成功后，控制台显示：

```
VITE v6.x ready in xxx ms → http://localhost:5173/
```

### 4.5 验证系统

打开浏览器访问 `http://localhost:5173`，应看到首页仪表盘。

---

## 五、测试命令

### 后端接口测试

```bash
python tests/test_backend.py
```

期望输出：`=== All tests passed ===`（33 个用例）

### 前端 E2E 测试

```bash
cd frontend
npm run test:e2e
```

期望输出：`E2E 全量巡检通过`

### 生产构建

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist/`。

---

## 六、部署到生产

### 6.1 前端生产构建

```bash
cd frontend
cp .env.production .env.local
npm run build
```

### 6.2 后端生产启动

```bash
# 关闭 reload，使用 production 配置
python -m uvicorn src.backend.main:app --host 0.0.0.0 --port 8000 --workers 1
```

### 6.3 反向代理（可选）

使用 Nginx 将 `/api` 转发到后端 `http://127.0.0.1:8000`，静态文件由 Nginx 直接提供。

---

## 七、常见问题

| 问题 | 原因 | 解决 |
|---|---|---|
| `ModuleNotFoundError: No module named 'ultralytics'` | 后端依赖未安装 | 执行 `pip install -r requirements-backend.txt` |
| `models/best_detect.pt not found` | 模型文件缺失 | 将 `best_detect.pt` 放入 `models/` |
| `npm run test:e2e` 浏览器启动失败 | Playwright 浏览器未安装 | `npx playwright install chromium` |
| 前端无法连接后端 | 跨域或服务未启动 | 确认后端已启动，开发环境已配置 Vite proxy |
| GitHub clone 失败 | 网络限制 | 使用离线 zip 包或更换网络环境 |

---

## 八、离线部署包清单

答辩演示前建议准备：

```
tea-yulu-offline/
├── tea-yulu-grading-platform/    # 项目源码 zip
├── models/                        # 模型权重（单独放置）
│   ├── best_detect.pt
│   ├── best_cls.pt
│   ├── training_curves.png
│   └── test_metrics.json
├── python-3.13.2-amd64.exe      # Python 安装包
├── node-v22.x-x64.msi           # Node.js 安装包
└── start_demo.bat               # 一键启动脚本
```

---

## 九、联系方式

- 项目仓库：`https://github.com/c8shakei/tea-yulu-grading-platform`
- 数据集：TeaLeafAgeQuality（Mendeley，DOI: 10.17632/7t964jmmy3）
