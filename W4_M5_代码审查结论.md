# W4 M5 里程碑审查结论

> 审查时间：2026-09-07  
> 审查人：项目经理助理  
> 审查范围：W4 提交的 M5 里程碑汇报、核心代码改动、`frontend/` 构建与 E2E 测试结果、GitHub 同步状态

---

## 一、总体评价

W4 M5 **核心目标全部完成，端到端主链路已跑通，GitHub 同步已完成**。M4 审查中提出的 P0/P1 问题（history 硬编码、E2E 硬编码路径、生产 baseURL、chunk 优化）均已修复，`npm run build` 成功，`npm run test:e2e` 全部通过。

| 维度 | 评分 | 说明 |
|---|---|---|
| 端到端联调 | 优 | 注册 → 登录 → 上传真实茶叶图片 → 后端推理 → 前端展示 → 溯源验证 → 历史记录，全链路可跑通 |
| M4 问题整改 | 优 | P0/P1 问题全部修复 |
| 构建与部署准备 | 优 | `npm run build` 成功，`VITE_API_BASE_URL` 已配置 |
| E2E 可移植性 | 优 | 已改用项目内 Playwright，`npm run test:e2e` 可直接运行 |
| 代码优化 | 良 | ECharts 按需引入，ConfidenceLineChart chunk 从 1038kB 降至 580kB |
| GitHub 同步 | 优 | 已推送至 `https://github.com/c8shakei/tea-yulu-grading-platform` |

---

## 二、P0/P1 任务完成确认

| 编号 | 任务 | 状态 | 验证方式 |
|---|---|---|---|
| M5-W4-01 | history 页 Schema 驱动化 | ✅ | 检查 `src/backend/ui_schema.json` 已补全 `pie-chart` 和 `line-chart`；`HistoryView.vue` 已改为纯 `SchemaRender` |
| M5-W4-02 | detect 端到端联调 | ✅ | E2E 中真实茶叶图片返回 `一级 / 92.10% / trace_id` |
| M5-W4-03 | trace 端到端联调 | ✅ | E2E 中用真实 trace_id 查询，`哈希链验证通过` |
| M5-W4-04 | 生产 baseURL 配置 | ✅ | `request.js` 读取 `VITE_API_BASE_URL`；`.env.development`/`.env.production` 已添加 |
| M5-W4-05 | 项目同步到 GitHub | ✅ | 已推送至 `https://github.com/c8shakei/tea-yulu-grading-platform` |
| M5-W4-06 | E2E 可移植化 | ✅ | `@playwright/test` + `playwright-core` 已加入 devDependencies；硬编码路径已移除 |
| M5-W4-07 | ECharts 按需引入 | ✅ | 新增 `src/utils/echarts.js`；ConfidenceLineChart chunk 从 1038kB 降至 580kB |

---

## 三、关键验证结果

### 3.1 构建验证

```bash
cd frontend
npm run build
```

结果：**构建成功**。仅 Element Plus 与 ECharts 组件 chunk 大于 500 kB 警告，不影响运行。

### 3.2 E2E 验证

```bash
cd frontend
npm run test:e2e
```

结果：**全部通过**。关键输出：
- `detect trace_id: c190c91e56974744a78d8893dee1f58e`
- 检测结果：`等级：一级 / 置信度：92.10%`
- 溯源验证：`哈希链验证通过`

### 3.3 GitHub 同步验证

```bash
gh repo create tea-yulu-grading-platform --public --source=. --remote=origin --push
```

结果：**成功推送**。
- 仓库地址：https://github.com/c8shakei/tea-yulu-grading-platform
- 本地分支 `main` 已跟踪 `origin/main`
- `.gitignore` 已排除 `.pt`、`node_modules/`、`dist/`、`datasets/` 等大文件

---

## 四、代码改动核对

| 文件 | 改动说明 |
|---|---|
| `src/backend/ui_schema.json` | `history.widgets` 补全 `pie-chart` 和 `line-chart` |
| `frontend/src/pages/HistoryView.vue` | 移除硬编码图表，改为纯 `SchemaRender` 渲染 |
| `frontend/src/components/SchemaRender.vue` | 修复 `handleUpload({ file })` → `handleUpload(uploadFile)`，解决文件上传 undefined 问题 |
| `frontend/src/utils/request.js` | `baseURL` 改为读取 `VITE_API_BASE_URL` |
| `frontend/.env.development` | 新增，开发环境 baseURL 为空 |
| `frontend/.env.production` | 新增，生产环境 baseURL 可配置 |
| `frontend/package.json` | 新增 `@playwright/test`、`playwright-core` devDependencies 和 `test:e2e` 脚本 |
| `frontend/tests/e2e/manual_check.cjs` | 移除硬编码 Playwright 路径，改用 `require('playwright-core')` |
| `frontend/src/utils/echarts.js` | 新增，按需注册 ECharts 组件 |
| `frontend/src/components/charts/*.vue` | 改为从 `src/utils/echarts.js` 引入 |

---

## 五、残留风险与下一步

### 5.1 残留风险

| 风险 | 影响 | 建议 |
|---|---|---|
| 训练曲线仍为静态 PNG | 低 | 当前为 W2 真实产物，答辩足够；动态化列为可选 |
| 移动端响应式未做 | 低 | 列为答辩前收尾项 |
| Element Plus chunk 1088kB | 低 | 不影响运行，可按需引入优化 |

### 5.2 下一步

1. **建议组长批复 M5 通过**。
2. **W4 进入答辩前收尾阶段**：移动端响应式适配、UI 细节打磨、README 完善。
3. **W5 介入**：使用 `npm run test:e2e` 跑全量巡检，验证不同环境可复现性。
4. **W2 接口（可选）**：如希望训练曲线动态化，W2 提供 `results.csv` 或训练日志 JSON 接口。

---

## 六、结论

**W4 M5 里程碑审查通过。** 端到端主链路已跑通，M4 审查问题已修复，构建与 E2E 测试通过，GitHub 仓库已成功同步。

**建议组长批复 M5 通过，W4 进入答辩前收尾阶段。**
