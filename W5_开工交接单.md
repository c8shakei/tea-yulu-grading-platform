# W5 开工交接单：测试与集成

**交接人：** 组长  
**接收人：** W5（测试与集成工程师）  
**交接日期：** 2026-09-07  
**阶段：** M6 — 答辩前全量测试与集成收尾

---

## 一、项目当前状态

| 里程碑 | 负责人 | 状态 | 关键产物 |
|---|---|---|---|
| M1 数据就绪 | W1 | ✅ 通过 | `datasets/tea_yulu/`（junction 到 D 盘） |
| M2 模型训练 | W2 | ✅ 通过 | `models/best_detect.pt` 为主模型，mAP50-95=0.702 |
| M3 后端与溯源 | W3 | ✅ 通过 | `src/backend/`，11 个接口，哈希链 |
| M4 前端搭建 | W4 | ✅ 通过 | `frontend/`，9 个页面，SchemaRender |
| M5 端到端联调 | W4 | ✅ 复验通过 | 真实茶叶图片 E2E 跑通（特级/73.61%） |
| M6 测试与集成 | W5 | 🔄 待开工 | 本交接单目标 |

---

## 二、W5 核心任务

### 2.1 后端接口全量测试

在 `tests/test_backend.py` 基础上扩展，覆盖以下场景：

| 接口 | 必测场景 |
|---|---|
| `POST /api/auth/register` | 正常注册、重复用户名、弱密码校验 |
| `POST /api/auth/login` | 正常登录、密码错误、用户不存在 |
| `GET /api/auth/me` | 带 token 访问、token 失效/伪造 |
| `POST /api/auth/logout` | 登出后 token 失效 |
| `POST /api/detect` | 匿名检测、登录后检测关联 user_id、真实茶叶图片、超大文件（>10MB）、非图片文件 |
| `GET /api/detections` | 仅返回当前用户记录、无记录时返回空列表 |
| `GET /api/ui/schema` | 返回有效 JSON、包含 9 个页面 |
| `GET /api/stats/overview` | 字段完整、总检测数正确 |
| `GET /api/stats/user/{id}` | 仅返回该用户统计、跨用户隔离 |
| `POST /api/trace` | 登录用户生成溯源、未登录拒绝 |
| `GET /api/trace/{id}` | 返回完整哈希链、验证通过 |

### 2.2 前端 E2E 可复现性验证

**关键要求：从 GitHub 仓库 clone 到干净目录，按 README 启动，跑通 E2E。**

```bash
# 步骤 1：clone 仓库（必须换一个干净目录，不得在原有 workspace 上覆盖）
git clone https://github.com/c8shakei/tea-yulu-grading-platform.git /tmp/tea-yulu-check

# 步骤 2：准备模型权重
# 从本地 models/ 复制 .pt 文件到 /tmp/tea-yulu-check/models/
# 或按 README 说明下载

# 步骤 3：启动后端
cd /tmp/tea-yulu-check
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements-backend.txt
python scripts/start_backend.py

# 步骤 4：启动前端
cd /tmp/tea-yulu-check/frontend
npm install
npm run dev

# 步骤 5：运行 E2E
npm run test:e2e
```

### 2.3 真实茶叶图片验证

- `frontend/tests/e2e/test_tea.jpg` 已被 `.gitignore` 排除，不会随仓库下发。
- W5 在 clone 后的目录中，必须从 `datasets/tea_yulu/test/images/` 复制一张真实茶叶图片到 `frontend/tests/e2e/test_tea.jpg`。
- 运行 E2E，确认检测结果不是「等外/0%」，且 `trace_id` 与哈希链验证通过。

### 2.4 跨用户数据隔离验证

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 注册用户 A，登录后上传真实茶叶图片检测 | 生成 trace_id_A |
| 2 | 注册用户 B，登录后上传另一张真实茶叶图片检测 | 生成 trace_id_B |
| 3 | B 调用 `GET /api/detections` | 只能看到 B 自己的记录，看不到 A 的 |
| 4 | B 调用 `GET /api/stats/user/{B_id}` | 统计只含 B 的数据 |
| 5 | B 调用 `GET /api/trace/{trace_id_A}` | 应返回 404 或提示无权限 |

### 2.5 移动端响应式初步检查

使用 Playwright 或 Chrome DevTools 模拟以下尺寸，记录问题：

| 设备 | 宽度 |
|---|---|
| iPhone SE | 375px |
| iPhone 12 Pro | 390px |
| iPad Mini | 768px |
| 笔记本 | 1280px |

重点检查：`/detect` 上传区域、`/history` 表格、`/trace` 时间线是否可读。

---

## 三、W5 交付物

| 文件 | 说明 |
|---|---|
| `W5_M6_集成报告.md` | 全链路复现步骤、能否演示、结论 |
| `W5_M6_测试报告.md` | 用例数、通过/失败数、关键指标 |
| `W5_M6_缺陷清单.md` | 按 W2/W3/W4 分类，含严重等级 |
| `W5_M6_答辩前测试结论.md` | 能否答辩演示、遗留风险、必须修复项 |

---

## 四、缺陷上报格式

发现缺陷后，按以下格式记录到 `W5_M6_缺陷清单.md`，并同步给对应员工：

```markdown
| 编号 | 模块 | 严重等级 | 复现步骤 | 预期结果 | 实际结果 | 归属 |
|------|------|----------|----------|----------|----------|------|
| BUG-01 | 后端 | P0 | ... | ... | ... | W3 |
```

严重等级：P0 阻断 / P1 高风险 / P2 中风险 / P3 低风险

---

## 五、启动与测试命令速查

```bash
# 后端启动
cd C:\Users\wzd\Desktop\毕业设计
C:\Users\wzd\.workbuddy\binaries\python\envs\tea-backend\Scripts\python.exe scripts/start_backend.py

# 前端启动
cd C:\Users\wzd\Desktop\毕业设计\frontend
npm run dev

# 后端测试
C:\Users\wzd\.workbuddy\binaries\python\envs\tea-backend\Scripts\python.exe -m pytest tests/test_backend.py -q

# 前端 E2E
npm run test:e2e

# 生产构建
npm run build
```

---

## 六、阻塞上报口径

遇到以下情况立即上报组长：

1. 从 GitHub clone 后按 README 无法启动后端或前端
2. `npm run test:e2e` 在干净目录上跑不通
3. 真实茶叶图片检测失败或返回异常结果
4. 发现跨用户数据隔离漏洞
5. 哈希链验证在未被篡改情况下失败

---

## 七、验收标准

W5 M6 通过条件：

1. 后端接口测试 ≥ 30 个用例，通过率 ≥ 95%
2. 前端 E2E 在干净 clone 目录上跑通
3. 真实茶叶图片端到端链路跑通
4. 跨用户数据隔离验证通过
5. `W5_M6_缺陷清单.md` 中 P0/P1 缺陷 ≤ 3 个或已全部指派修复
6. `W5_M6_答辩前测试结论.md` 明确：当前版本可答辩演示

---

**组长签字：** 组长  
**交接时间：** 2026-09-07 08:55 CST
