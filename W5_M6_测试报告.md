# W5 M6 测试报告

**报告人：** W5（测试与集成工程师）  
**接收人：** 组长  
**日期：** 2026-09-07  
**阶段：** M6 — 答辩前全量测试与集成收尾  

---

## 一、测试范围

| 层级 | 测试内容 |
|---|---|
| 后端接口 | 11 个 API 契约：认证（register/login/me/logout）、检测（detect）、历史（detections）、统计（stats/overview、stats/user/{id}）、UI Schema（ui/schema）、溯源（trace、trace/{id}） |
| 前端 E2E | 注册、登录、登出、真实茶叶检测、历史、溯源、个人中心、低代码、训练曲线、负向用例 |
| 数据隔离 | 跨用户 detections / stats / trace 访问控制 |
| 响应式 | iPhone SE / iPhone 12 Pro / iPad Mini / Laptop 四种尺寸下 /detect、/history、/trace 可读性 |

---

## 二、后端接口测试结果

**执行命令：**

```bash
cd "C:\Users\wzd\Desktop\毕业设计"
"C:\Users\wzd\.workbuddy\binaries\python\envs\tea-backend\Scripts\python.exe" -m pytest tests/test_backend.py -q
```

**执行结果：**

```text
32 passed, 2 warnings in 11.48s
```

> 注：测试脚本使用固定路径临时 SQLite 数据库（`C:/Users/wzd/AppData/Local/Temp/tea_yulu_test/test_backend_m6.db`）。早期使用 `tempfile.NamedTemporaryFile` 在 Windows 下出现并发 SQLite 访问异常，已改为常规文件方式解决。

**用例覆盖统计：**

| 接口 | 用例数 | 关键场景 |
|---|---|---|
| /health | 1 | 健康检查 |
| POST /api/auth/register | 4 | 正常注册、重复用户名、弱密码、空字段 |
| POST /api/auth/login | 3 | 正常登录、密码错误、用户不存在 |
| GET /api/auth/me | 3 | 有效 token、无效 token、无 token |
| POST /api/auth/logout | 1 | 登出确认 |
| POST /api/detect | 6 | 匿名检测、匿名隔离、登录关联、真实茶叶图片、超大文件拒绝、非图片文件 |
| GET /api/detections | 3 | 仅当前用户、空列表、未授权拒绝 |
| GET /api/ui/schema | 1 | Schema 结构与 7 个导航页 |
| GET /api/stats/overview | 1 | 字段完整性与总数正确 |
| GET /api/stats/user/{id} | 2 | 仅当前用户、跨用户隔离 403 |
| POST /api/trace | 2 | 追加复核、未登录拒绝 |
| GET /api/trace/{id} | 3 | 链完整性、越权访问记录、篡改检测 |
| 跨用户隔离 | 1 | A/B 用户完全隔离 |
| 模型存在性 | 1 | `models/best_detect.pt` 存在 |
| **合计** | **32** | 通过率 **100%** |

---

## 三、前端 E2E 测试结果

**执行环境：** 干净 clone 目录 `C:\Users\wzd\AppData\Local\Temp\tea-yulu-check`

**执行命令：**

```bash
cd "C:\Users\wzd\AppData\Local\Temp\tea-yulu-check\frontend"
npm run test:e2e
```

**结果：** 通过

| 检查项 | 结果 |
|---|---|
| 未登录访问首页 | 通过 |
| 未登录访问 /history 重定向到 /login | 通过 |
| 注册 → 登录 | 通过 |
| 首页仪表盘图表渲染 | 通过 |
| 检测工作台上传真实茶叶图片 | 通过（特级 / 73.61%） |
| 历史记录渲染 | 通过 |
| 溯源验证（trace_id / 哈希链） | 通过 |
| 个人中心 | 通过 |
| 低代码演示 | 通过 |
| 训练曲线看板 | 通过 |
| 未登录访问 /detect 重定向 | 通过 |
| 超大文件前端拦截 | 通过 |
| 非图片文件判为等外/0% | 通过 |

---

## 四、响应式测试结果

**脚本：** `C:\Users\wzd\AppData\Local\Temp\tea-yulu-check\frontend\tests\e2e\responsive_check.cjs`

| 设备 | 宽度 | /detect | /history | /trace |
|---|---|---|---|---|
| iPhone SE | 375px | 关键文本可见 | 关键文本可见，表头截断 | 关键文本可见 |
| iPhone 12 Pro | 390px | 关键文本可见 | 关键文本可见，表头截断 | 关键文本可见 |
| iPad Mini | 768px | 关键文本可见 | 关键文本可见 | 关键文本可见 |
| Laptop | 1280px | 关键文本可见 | 关键文本可见 | 关键文本可见 |

**问题记录：** `/history` 页面在手机宽度下数据列表列名换行/截断（`class_name` 显示为 `class_n ame`），不影响功能但影响美观。

---

## 五、关键指标

| 指标 | 目标 | 实际 | 结论 |
|---|---|---|---|
| 后端接口用例数 | ≥ 30 | 32 | 达成 |
| 后端接口通过率 | ≥ 95% | 100% | 达成 |
| 前端 E2E 可复现性 | 干净目录跑通 | 通过 | 达成 |
| 真实茶叶图片端到端 | 非等外/0% | 特级 / 73.61% | 达成 |
| 跨用户数据隔离 | B 看不到 A | 已通过 | 达成 |
| 响应式初步检查 | 4 种尺寸记录 | 已记录 | 达成 |

---

## 六、遗留问题

详见 `W5_M6_缺陷清单.md`。主要遗留：

1. `/api/trace/{id}` 当前为公开读取，任何用户可查询任意 trace（需 W3 评估是否按数据隔离契约加固）。
2. `/history` 数据列表在手机尺寸下表头换行/截断。
3. `profile` 页面总检测数显示为 0，疑似未正确刷新当前用户统计。
4. 后端 HTTPException 返回 `{detail: ...}` 而非统一 `{code, message, data}`。
