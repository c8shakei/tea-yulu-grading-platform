# W2 M2 里程碑完工审查结论

## 审查对象

W2 视觉模型工程师提交的 M2 模型训练里程碑材料（6 份）：

| 序号 | 文件名 | 作用 |
|---|---|---|
| 1 | `W2_M2_过程性资料追溯档案.docx` | 训练平台、数据集、代码版本、变更控制、产物清单、问题修复、追溯索引 |
| 2 | `W2_M2里程碑汇报_版本说明.md` | 三个版本文件的对照说明与选用建议 |
| 3 | `W2_M2里程碑汇报_模型训练.docx` | 原始归档（仅 yolov8n 基线） |
| 4 | `W2_M2里程碑汇报_模型训练_v1_yolov8n基线.docx` | v1，同原始归档 |
| 5 | `W2_M2里程碑汇报_模型训练_v2_yolov8s消融完成.docx` | **当前提交组长审阅版** |
| 6 | `W2_M2里程碑汇报_模型训练_提交组长版.docx` | 提交组长的正式汇报信函 |

## 实际交付物核验

已核对本地 `models/` 目录，4 个核心权重与指标文件均存在：

| 文件 | 大小 | 说明 |
|---|---|---|
| `best_detect.pt` | 6.0M | yolov8n 检测主模型权重 |
| `best_cls.pt` | 2.9M | yolov8n-cls 分类对照权重 |
| `best_detect_yolov8s.pt` | 22M | yolov8s 检测消融权重 |
| `best_cls_yolov8s-cls.pt` | 9.8M | yolov8s-cls 分类消融权重 |
| `test_metrics.json` / `val_metrics.json` | — | yolov8n 检测/分类指标 |
| `test_metrics_yolov8s.json` / `val_metrics_yolov8s-cls.json` | — | yolov8s 消融指标 |
| `training_curves.png` / `training_curves_yolov8s.png` | — | 训练曲线 |
| `results.csv` / `results_yolov8s.csv` / `results_yolov8s-cls.csv` | — | 训练日志 |

## 关键指标

### 检测任务对比

| 模型 | mAP50-95 | mAP50 | Precision | Recall |
|---|---|---|---|---|
| yolov8n（基线/主模型） | 0.702 | 0.880 | 0.799 | 0.867 |
| yolov8s（消融） | 0.743 | 0.906 | 0.884 | 0.851 |
| 变化 | **+0.041** | +0.026 | +0.085 | -0.016 |

### 分类任务对比

| 模型 | Top-1 Accuracy | Top-5 Accuracy |
|---|---|---|
| yolov8n-cls（基线） | 0.904 | 1.000 |
| yolov8s-cls（消融） | 0.923 | 1.000 |
| 变化 | **+0.018** | 持平 |

### 目标达成情况

- 检测模型目标 mAP50-95 ≥ 0.90：**未达成**（0.743，差距 0.157）
- 分类对照目标 Top-1 ≥ 0.90：**已达成**（0.904/0.923）
- 特级叶检测 mAP50-95：0.534（yolov8n）→ 0.558（yolov8s），仍偏低

## 变更控制状态

| 变更编号 | 内容 | 状态 |
|---|---|---|
| CR-W1-002 | 训练脚本支持 yolov8s 多规模；产物加 size 后缀物理隔离 | 已批复并执行 |
| CR-W1-003 | 训练平台由 Colab 迁移至 Kaggle；脚本可移植化改造 | 已批复并执行 |

- yolov8n 基线产物未被覆盖，yolov8s 产物均带 `_yolov8s` 后缀，符合批复约束。
- 接口契约②无变更，`detect(image)` 输出仍为：
  ```json
  {"class_id": int, "class_name": str, "confidence": float, "bbox": [x1, y1, x2, y2]}
  ```

## 阻塞风险

| 风险 | 现状 | 影响 |
|---|---|---|
| 检测 mAP50-95 未达 0.90 | yolov8s 消融后仍仅 0.743 | 影响论文实验指标达标，需在 W3/W4 通过数据增强/后处理补偿 |
| 特级类检测能力弱 | 特级 mAP50-95 仅 0.558 | 影响高端茶叶分级准确率，需重点优化 |
| yolov8s 是否升主模型 | 提升幅度 0.041，未满足二次裁定口径（≥0.05 且 CPU 单张 <200ms） | 需组长决策是否切换 |

## 待组长决策项

1. **是否确认 W2 M2 里程碑完成，批准进入 W3 后端集成 / W4 前端溯源阶段？**
2. **是否将 yolov8s 升为主模型？**
   - 当前提升 0.041，未满足 CR-W1-002 批复中“提升 ≥0.05 且 CPU 单张 <200ms”的二次裁定口径。
   - 建议：维持 yolov8n 主模型，yolov8s 作为消融对照写入论文。
3. **是否继续追加特级类优化实验？**
   - 选项 A：继续让 W2 做更多数据增强 / Mosaic / Mixup 实验。
   - 选项 B：将特级优化延至 W3/W4，通过后处理（如多尺度推理、NMS 调参、置信度阈值优化）补偿。
   - 建议：选 B，优先推进 W3/W4 集成，避免阻塞整体工期。

## 下一步建议

- **批准 W2 M2 里程碑完成**，表扬训练平台迁移与消融实验按期完成。
- **维持接口契约②不变**，W3 继续按 `best_detect.pt`（yolov8n）对接。
- **W3 立即开工**：后端 FastAPI、SQLite、哈希链、认证接口、统计接口。
- **W4 立即开工**：Vue 多页面、ECharts 可视化、低代码 Schema 演示页面。
- **特级指标优化与 yolov8s 主模型切换**延至后续迭代中根据 W3/W4 实测效果再决策。

## 备注

- 已提取全部 docx 文本与表格进行核对；原始文件未修改。
- Kaggle notebook 公开链接已由 PM 于 2026-09-05 回填：`https://www.kaggle.com/code/wzdnmu/notebook357866ef88`。
