# 数据说明 — 恩施玉露茶品质分级数据集

> 本说明由 W1（数据工程工程师）编制，用于记录数据集来源、授权、预处理与划分方式。

---

## 1. 数据集来源

- **数据集名称**：TeaLeafAgeQuality: Age-Stratified Tea Leaf Quality Classification Dataset
- **发布平台**：Mendeley Data
- **DOI**：`10.17632/7t964jmmy3`
- **版本**：Version 1
- **许可证**：CC BY 4.0（署名 4.0 国际）
- **引用方式**：
  > Md Mohsin Kabir, Md Sadman Hafiz, Shattik Bandyopadhyaa, Jamin Rahman Jim, Firoz Mridha. "TeaLeafAgeQuality: Age-Stratified Tea Leaf Quality Classification Dataset". Mendeley Data, V1, 2024. DOI: 10.17632/7t964jmmy3.1
- **本地下载路径**：`D:\BISHE_DATA\raw\TeaLeafAgeQuality.zip`
- **原始说明**：数据集按茶叶嫩度分为 4 个类别，对应采摘后 1–2 天（T1）、3–4 天（T2）、5–7 天（T3）、7 天以上（T4）。Mendeley 页面提供 raw、annotated、augmented 三个子版本；本项目使用 **annotated 版本**（含 YOLO 格式标注），并自行用 albumentations 做增强，不直接使用其 augmented 版本。

---

## 2. 类别映射

与接口契约①严格一致：

| 原数据集类别（Raw 目录名）       | class_id | 中文等级 |
|--------------------------------|----------|----------|
| Category A（采摘后 1–2 天）     | 0        | 特级     |
| Category B（采摘后 2–4 天）     | 1        | 一级     |
| Category C（采摘后 4–7 天）     | 2        | 二级     |
| Category D（7 天以上，不宜制茶） | 3        | 等外     |

> 2026-09-04 实测确认：annotated 版本标注的 class_id 与 Raw 版本 A/B/C/D 四类一一对应（各类标注框数 551/616/508/520 与 Raw 各类图像数 562/615/508/523 基本吻合），id 无需重排，仅做中文命名映射。

映射逻辑：`scripts/w1_extract_prepare.py` 读取 YOLO 标注的 class_id 作为最终类别；若目录名含类别提示且与标注冲突，则跳过该样本。

---

## 3. 目录结构

```text
D:\BISHE_DATA\datasets\tea_yulu
├── all
│   ├── images
│   └── labels
├── train
│   ├── images
│   └── labels
├── val
│   ├── images
│   └── labels
├── test
│   ├── images
│   └── labels
├── data.yaml
├── split_manifest.csv
└── class_distribution.csv
```

> 为缓解 C 盘空间压力（C 盘仅剩约 6 GB），实际数据存储在 `D:\BISHE_DATA\datasets\tea_yulu`；项目根目录通过 Windows 目录 junction 映射到 `datasets/tea_yulu`，保证接口契约路径不变。该方案已通过变更 `CR-W1-001` 有条件批准。

---

## 4. D 盘存储与 junction 映射（CR-W1-001）

### 4.1 原因
C 盘剩余空间仅约 6 GB，无法容纳 4.59 GB 压缩包、解压后图像、增强输出及中间文件。D 盘可用空间约 278 GB，因此将实际数据存放到 D 盘。

### 4.2 路径映射

| 作用 | 路径 | 说明 |
|------|------|------|
| 实际数据目录 | `D:\BISHE_DATA\datasets\tea_yulu` | 真实存储 images/labels/data.yaml |
| 项目接口路径 | `C:\Users\wzd\Desktop\毕业设计\datasets\tea_yulu` | Windows 目录 junction，逻辑上等同于 D 盘目录 |

### 4.3 一键建链
运行 `scripts/setup_data_link.bat` 即可自动：
1. 检查 `D:\BISHE_DATA\datasets\tea_yulu` 是否存在；
2. 移除旧的 `datasets/tea_yulu`（如有）；
3. 执行 `mklink /J` 创建 junction；
4. 校验 `data.yaml`、`train/images`、`val/images`、`test/images` 是否可达。

> junction 属于本地环境状态，**不进 Git**；换机或重装后重新运行该脚本即可恢复。

---

## 5. 预处理流程

1. **下载**：使用 `scripts/w1_download_dataset.py` 从 Mendeley Data 并行分片下载全量 zip（约 4.59 GB），支持断点续传。
2. **解压与提取**：`scripts/w1_extract_prepare.py` 自动识别 annotated 子集，过滤 augmented/raw 版本。
3. **类别对齐**：将 T1–T4 统一映射为 0–3，并重写所有 YOLO 标注文件的 class_id。
4. **划分**：`scripts/w1_split_dataset.py` 按 7:2:1 分层随机划分为 train/val/test，固定随机种子 `SEED=42`。
5. **增强**：`scripts/w1_augment.py` 仅对 train 集做几何/光度增强（水平翻转、随机旋转、亮度对比度、HSV、高斯噪声/模糊等），并同步更新 bbox 坐标。

---

## 6. 划分比例与规模

> 以下数值为 2026-09-04 实际运行结果（已通过全量校验：图像/标注一一对应，0 条异常标注行）。

| 子集   | 比例 | 样本数（原始） | 样本数（增强后） |
|--------|------|----------------|------------------|
| train  | 70%  | 1537           | 4611（1537 原图 + 3074 增强，AUG_FACTOR=2） |
| val    | 20%  | 439            | 439（不增强）    |
| test   | 10%  | 219            | 219（不增强）    |
| 合计   | 100% | 2195           | 5269             |

**类别分布（按 bbox 计）**：

| 子集  | 特级(0) | 一级(1) | 二级(2) | 等外(3) |
|-------|---------|---------|---------|---------|
| train | 1170    | 1302    | 1071    | 1092    |
| val   | 111     | 123     | 102     | 104     |
| test  | 57      | 63      | 50      | 52      |
| 原始 all | 551  | 616     | 508     | 520     |

> 源数据共 2208 对图像/标注，其中 13 张标注文件为空（无目标框的背景图），已跳过；实际入册 2195 对，跳过率 0.59%。

类别分布详见 `split_manifest.csv` 与 `class_distribution.csv`。

---

## 7. Colab 取数方案（供 W2 使用）

冻结约束规定**模型训练在 Colab GPU 上进行**，而 junction 仅在本地 Windows 生效。因此 W2 在 Colab 上应直接使用数据集目录本身，而非 junction。推荐以下任一方式：

### 方案 A：上传 Google Drive（推荐）
1. 将 `D:\BISHE_DATA\datasets\tea_yulu` 整个文件夹压缩后上传到 Google Drive（如 `MyDrive/Colab/tea_yulu`）。
2. 在 Colab Notebook 中挂载 Drive：
   ```python
   from google.colab import drive
   drive.mount('/content/drive')
   ```
3. 复制或软链到 `/content/datasets/tea_yulu`：
   ```bash
   !mkdir -p /content/datasets
   !ln -s /content/drive/MyDrive/Colab/tea_yulu /content/datasets/tea_yulu
   ```
4. 训练时直接使用便携版 `data.yaml`（已在数据集根目录中）：
   ```python
   from ultralytics import YOLO
   model = YOLO('yolov8n.pt')
   model.train(data='/content/datasets/tea_yulu/data.yaml', epochs=100, imgsz=640)
   ```

### 方案 B：Colab 内直接从 Mendeley 下载
1. 在 Colab 中运行 `w1_download_dataset.py` 与 `w1_extract_prepare.py` 重新下载并准备数据；
2. 运行 `w1_split_dataset.py` 生成 `data.yaml`；
3. 该方式耗时较长（约 20–30 分钟），但可完全复现 W1 的处理流程。

> 无论采用哪种方式，`data.yaml` 中均不得出现 `D:\` 等盘符绝对路径；数据集根目录内的 `data.yaml` 使用 `path: .`，可随文件夹被复制到任意位置后直接使用。

---

## 8. 已知问题与注意事项

1. **C 盘空间不足**：C 盘仅剩约 6 GB，无法容纳 4.59 GB zip + 解压后图像 + 增强输出。已通过 CR-W1-001 采用 D 盘存储 + junction 映射方案。
2. **数据集子版本选择**：zip 内包含 raw、annotated、augmented 三个版本；脚本选择 annotated 版本（2208 对）并自行增强，未使用其 augmented 版本（5298 对），避免与 W1 任务中"自行做 albumentations 增强"的要求冲突。
3. **空标注样本**：13 张图像的标注文件为空（无目标框的背景图），已跳过未入册，跳过率 0.59%。如需将其作为负样本引入检测训练，可由 W2 决策后另行处理。
4. **标注语义**：若原数据集标注为整图分类框（部分 bbox 为全图范围），YOLO 训练时会按目标检测任务处理；W2 可据此决定是否使用 `yolov8-cls` 分类模型或保留检测模型。该决策需 W2 与组长确认。
5. **类别基本均衡**：四类样本 551/616/508/520，最大/最小比约 1.21，无需额外类别均衡处理。
6. **Git 忽略**：数据集目录已加入 `.gitignore`，不会被提交到版本库。

---

## 9. 复现方式

```bash
cd scripts
python w1_download_dataset.py      # 下载 zip
python w1_extract_prepare.py       # 解压提取 + 类别映射
python w1_split_dataset.py         # 7:2:1 分层划分
python w1_augment.py               # 仅增强 train 集
setup_data_link.bat                # 创建 junction（Windows）
```

随机种子固定为 `42`，三次运行结果一致。

---

## 10. 交付物清单

- `scripts/w1_download_dataset.py`
- `scripts/w1_extract_prepare.py`
- `scripts/w1_split_dataset.py`
- `scripts/w1_augment.py`
- `scripts/setup_data_link.bat`
- `data.yaml`（项目根目录主文件，`path: datasets/tea_yulu`）
- `D:\BISHE_DATA\datasets\tea_yulu\data.yaml`（便携副本，`path: .`，供 Colab 使用）
- `D:\BISHE_DATA\datasets\tea_yulu\split_manifest.csv`（逐样本清单，2195 行：filename/split/class_id/class_name）
- `D:\BISHE_DATA\datasets\tea_yulu\split_summary.csv`（split × class 汇总统计）
- `D:\BISHE_DATA\datasets\tea_yulu\class_distribution.csv`
- `数据说明.md`
- `.gitignore`
