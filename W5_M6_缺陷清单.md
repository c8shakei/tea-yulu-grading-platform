# W5 M6 缺陷清单

**报告人：** W5（测试与集成工程师）  
**接收人：** 组长  
**日期：** 2026-09-07  
**阶段：** M6 — 答辩前全量测试与集成收尾  

---

## 缺陷汇总

| 编号 | 模块 | 严重等级 | 归属 | 状态 |
|---|---|---|---|---|
| BUG-01 | 后端 | P1 | W3 | **已修复（组长现场修复并验证）** |
| BUG-02 | 后端 | P3 | W3 | 待修复 |
| BUG-03 | 后端 | P2 | W3 | 待修复 |
| BUG-04 | 前端 | P2 | W4 | 待修复 |
| BUG-05 | 前端 | P2 | W4 | 待修复 |
| BUG-06 | 集成/网络 | P2 | W5 / 组长协调 | 记录 |
| BUG-07 | 文档/预期 | P3 | W4 / 组长 | **已确认** |

---

## 缺陷详情

### BUG-01：/api/trace/{id} 读取未做用户隔离

| 项目 | 内容 |
|---|---|
| **模块** | 后端 |
| **严重等级** | P1（高风险） |
| **归属** | W3 |
| **复现步骤** | 1. 用户 A 登录后上传图片生成 trace_id_A。<br>2. 用户 B 登录。<br>3. B 调用 `GET /api/trace/{trace_id_A}`。 |
| **预期结果** | 返回 404 或 403，提示无权限。 |
| **实际结果** | 返回完整哈希链，status_code = 200。 |
| **修复方式** | 组长在 `src/backend/traceability.py::get_trace` 中增加 `require_user` 依赖；匿名 trace（user_id 为 NULL）允许任何登录用户读取，已认证用户的 trace 仅 owner 或 admin 可读取。同步更新 `tests/test_backend.py` 增加跨用户隔离用例。 |
| **修复验证** | `tests/test_backend.py` 32 个用例全部通过（含新增 `test_trace_isolation`）；前端 `npm run test:e2e` 全量通过。 |
| **依据** | 交接单 2.4 跨用户数据隔离：B 调用 GET /api/trace/{trace_id_A} 应返回 403 或提示无权限。 |

---

### BUG-02：HTTPException 未纳入统一响应格式

| 项目 | 内容 |
|---|---|
| **模块** | 后端 |
| **严重等级** | P3（低风险） |
| **归属** | W3 |
| **复现步骤** | 1. 调用 `POST /api/auth/register` 重复注册。<br>2. 调用 `POST /api/auth/login` 使用错误密码。 |
| **预期结果** | 返回统一格式 `{code, message, data}`。 |
| **实际结果** | 返回 `{detail: "username already exists"}` 或 `{detail: "Incorrect username or password"}`。 |
| **修复建议** | 在 `main.py` 的全局异常处理器中增加对 `HTTPException` 的捕获，统一转换为 `{code, message, data}`；或各路由显式抛出统一响应。 |
| **依据** | W3 M3 接口说明应保持一致的错误格式。 |

---

### BUG-03：注册接口未校验弱密码

| 项目 | 内容 |
|---|---|
| **模块** | 后端 |
| **严重等级** | P2（中风险） |
| **归属** | W3 |
| **复现步骤** | 1. 调用 `POST /api/auth/register`，密码为 "1"。 |
| **预期结果** | 返回 400，提示密码长度不足。 |
| **实际结果** | 注册成功并返回 token。 |
| **修复建议** | 在 `auth.py::register` 中增加密码长度/复杂度校验（建议 ≥ 6 位）。 |
| **依据** | 交接单 2.1 注册场景要求"弱密码校验"。 |

---

### BUG-04：/history 数据列表在手机尺寸下表头截断/换行

| 项目 | 内容 |
|---|---|
| **模块** | 前端 |
| **严重等级** | P2（中风险） |
| **归属** | W4 |
| **复现步骤** | 1. 使用 iPhone SE（375px）或 iPhone 12 Pro（390px）登录后访问 `/history`。 |
| **预期结果** | 数据列表列名完整可读，无换行截断。 |
| **实际结果** | `class_name` 显示为 `class_n ame`，`confidence` 显示为 `confide nce`，列头换行影响可读性。 |
| **修复建议** | 对 `/history` 数据表格增加响应式处理：小屏下使用更短列名、横向滚动或卡片列表替代表格。 |
| **依据** | 交接单 2.5 移动端响应式检查：/history 表格需可读。 |

---

### BUG-05：个人中心总检测数显示为 0

| 项目 | 内容 |
|---|---|
| **模块** | 前端 |
| **严重等级** | P2（中风险） |
| **归属** | W4 |
| **复现步骤** | 1. 登录后上传真实茶叶图片检测成功。<br>2. 访问 `/profile`。 |
| **预期结果** | 总检测数 ≥ 1，与个人统计一致。 |
| **实际结果** | 总检测数显示为 0（见 E2E 输出 `[profile] body: ... 0 总检测数 ...`）。 |
| **修复建议** | 检查 `ProfileView.vue` 中调用 `GET /api/stats/user/{id}` 的时机；确保进入页面时重新拉取最新用户统计，而不是展示静态初始值。 |
| **依据** | 已批复变更 CR-W1-004/005：个人中心应展示正确的用户统计。 |

---

### BUG-06：GitHub 直接 clone 在当前环境失败

| 项目 | 内容 |
|---|---|
| **模块** | 集成 / 网络环境 |
| **严重等级** | P2（中风险） |
| **归属** | W5（记录），需组长/PM 协调网络或备用方案 |
| **复现步骤** | 1. 在干净环境执行 `git clone https://github.com/c8shakei/tea-yulu-grading-platform.git <dir>`。 |
| **预期结果** | clone 成功。 |
| **实际结果** | `fatal: unable to access ... CONNECT tunnel failed, response 502`。 |
| **修复建议** | 答辩演示前确认目标网络可访问 GitHub；准备本地仓库 / zip 包 / GitHub 镜像作为备份。 |
| **依据** | 交接单 2.2 要求从 GitHub 仓库 clone 到干净目录。 |

---

### BUG-07：UI Schema 返回 7 个页面 vs 文档要求 9 个页面

| 项目 | 内容 |
|---|---|
| **模块** | 文档 / 前端 |
| **严重等级** | P3（低风险） |
| **归属** | W4 / 组长 |
| **复现步骤** | 1. 调用 `GET /api/ui/schema`。<br>2. 检查 `nav` 和 `pages` 长度。 |
| **预期结果** | 交接单 2.1 要求"包含 9 个页面"；W5 提示词列出 9 个页面（含 `/login`、`/register`）。 |
| **实际结果** | `nav` 含 7 项，`pages` 含 7 项；`/login` 和 `/register` 为硬编码路由，不在 schema 中。 |
| **处理结论** | **组长确认**：项目共 9 个页面，其中 7 个主业务页面（`/`、`/detect`、`/history`、`/trace`、`/profile`、`/lowcode`、`/training`）由 UI Schema 动态驱动；`/login` 和 `/register` 为认证前置页面，采用硬编码路由，不纳入 Schema。文档中"9 个页面"包含 login/register，schema 中 7 个页面为业务页面，两者不矛盾。 |
| **依据** | 交接单 2.1 与 W5 提示词对"9 个页面"的描述存在歧义，经组长裁定后统一口径。 |

---

## 风险说明

- **P0 阻断**：导致链路完全无法跑通。当前无 P0。
- **P1 高风险**：可能导致数据隔离失效或安全漏洞（BUG-01）。
- **P2 中风险**：影响演示体验或存在功能缺陷（BUG-03/04/05/06）。
- **P3 低风险**：文档格式或美观问题（BUG-02/07）。

当前 P0/P1 缺陷共 1 个（BUG-01），P2 缺陷 4 个，P3 缺陷 2 个；总计 7 个，在 M6 验收标准"P0/P1 缺陷 ≤ 3 个或已全部指派修复"范围内。
