# W4 M5 返工说明

**返工人：** W4（前端）  
**返工日期：** 2026-09-07  
**对应批复：** `W4_M5_组长批复.md`（有条件通过，24 小时内复验）  

---

## 返工总览

| 编号 | 问题 | 修复方式 | 验证结果 |
|------|------|----------|----------|
| P0-1 | E2E 使用纯色占位图 | 从真实数据集复制茶叶图片替换 `frontend/tests/e2e/test_tea.jpg` | `npm run test:e2e` 通过，真实检测输出 `特级 / 73.61%` 与 `trace_id` |
| P0-2 | M5 里程碑汇报 GitHub 状态矛盾 | 删除第 7/9 节阻塞描述，统一为「GitHub 仓库已创建并推送」 | 文档已自洽 |
| P0-3 | GitHub 仓库缺少 README | 新增 `README.md`，含简介、技术栈、目录结构、启动方式、环境变量、模型下载说明、贡献者与许可证 | 已提交到 `main` 分支 |
| P0-4 | 二进制文件已入仓 | 通过 GitHub API 从远程删除 `*.png`、`*.jpg`、`*.docx`、`*.xlsx`、`.workbuddy/`；本地同步删除并更新 `.gitignore` | 远程仓库已无上述二进制文件 |
| P1-3 | E2E 缺少负向用例 | 在 `manual_check.cjs` 新增 3 个负向用例 | 全量 E2E 通过 |

---

## 1. P0-1：E2E 测试图片替换

**原问题**  
`frontend/tests/e2e/test_tea.jpg` 为 824 字节纯色占位图，无法证明端到端对真实茶叶样本有效。

**修复**  
从本地数据集软链接指向的真实目录复制一张测试图片：

```bash
# 源路径（来自 TeaLeafAgeQuality 数据集划分后的测试集）
cp /d/BISHE_DATA/datasets/tea_yulu/test/images/20231224_161209_jpg_rf_488331cf39e61d4d6025a7eedab300f9.jpg \
   frontend/tests/e2e/test_tea.jpg
```

替换后文件大小为 74,826 字节，为真实茶叶照片。

**验证**  
运行 `npm run test:e2e`，真实检测输出示例：

```text
等级：特级
置信度：73.61%
边界框：[0.479, 0.293, 0.695, 0.450]
溯源 ID：1722fe5e4266430290adc5b99ffdd3f9
```

---

## 2. P0-2：里程碑汇报文档自洽性

**原问题**  
`W4_M5_里程碑汇报.md` 第 5 节写「GitHub 同步 ✅ 完成」，第 7 节与第 9 节又称「GitHub 阻塞」。

**修复**  
删除第 7 节中的 `GitHub 仓库创建/推送` 阻塞行，删除第 8 节中的 `D1/D2` 决策项，删除第 9 节中「GitHub 同步因权限问题阻塞」的描述。

**验证**  
全文搜索「GitHub 阻塞」「权限问题」已无匹配，文档口径统一为「已完成」。

---

## 3. P0-3：补充 README.md

**修复**  
新增 `README.md`，包含以下章节：

- 项目简介
- 技术栈
- 目录结构
- 环境要求
- 后端启动方式
- 前端启动方式
- 环境变量说明
- 模型权重下载说明
- 端到端测试说明
- 生产构建说明
- 许可证
- 贡献者

**验证**  
`gh api repos/c8shakei/tea-yulu-grading-platform/contents/README.md?ref=main --jq '.name'` 返回 `README.md`。

---

## 4. P0-4：清理已入仓二进制文件

**已删除文件**

| 文件 | 类型 | 说明 |
|------|------|------|
| `frontend/tests/e2e/test_tea.jpg` | 图片 | 占位测试图，已替换为本地真实图片（不再入仓） |
| `models/training_curves.png` | 图片 | 训练曲线 PNG |
| `models/training_curves_yolov8s.png` | 图片 | yolov8s 训练曲线 PNG |
| `*.docx` | Word 文档 | 过程性审批/汇报文档 |
| `*.xlsx` | Excel | 环境配置清单 |
| `.workbuddy/memory/*.md` | 工作记忆 | WorkBuddy 本地会话记录 |

**`.gitignore` 更新**  
新增规则，防止再次入仓：

```gitignore
frontend/tests/e2e/test_tea.jpg
frontend/tests/e2e/fixtures/
models/*.png
models/*.jpg
models/*.jpeg
*.pt
*.pth
*.onnx
*.docx
*.xlsx
*.pdf
.workbuddy/
```

**验证**  
远程仓库已无 `*.png`、`*.jpg`、`*.docx`、`*.xlsx`、`.workbuddy` 文件：

```bash
gh api repos/c8shakei/tea-yulu-grading-platform/git/trees/main?recursive=1 \
  --jq '.tree[].path' | grep -E '\.(png|jpg|jpeg|docx|xlsx|pdf)$|\.workbuddy'
# 无输出
```

---

## 5. P1-3：补充 E2E 负向用例

在 `frontend/tests/e2e/manual_check.cjs` 中新增：

| 编号 | 用例 | 预期行为 |
|------|------|----------|
| 12 | 未登录访问 `/detect` | 跳转至 `/login` |
| 13 | 上传 12MB 超大图片 | 前端拦截并提示「图片大小不能超过 10MB」 |
| 14 | 上传非图片 `.txt` 文件 | 后端判为最低等级「等外」，置信度 0% |

**验证**  
`npm run test:e2e` 全量通过，输出：

```text
E2E 全量巡检通过，截图保存到: ...\frontend\tests\e2e\screenshots
```

---

## 6. 附加优化

### 6.1 `.env.production` 给出默认值

将 `VITE_API_BASE_URL=` 改为 `VITE_API_BASE_URL=/api`，并保留注释说明。

### 6.2 `.gitignore` 排除 E2E fixtures

负向用例生成的 `oversize_12mb.jpg` 与 `not_an_image.txt` 位于 `frontend/tests/e2e/fixtures/`，已加入 `.gitignore`，不再入仓。

---

## 7. 复验结论

| 通过条件 | 状态 |
|----------|------|
| P0-1 ~ P0-4 全部修复 | ✅ |
| `npm run build` 成功 | ✅ |
| `npm run test:e2e` 在真实茶叶图片上跑通，且新增负向用例通过 | ✅ |
| GitHub 仓库可见 README.md，且二进制文件已移除 | ✅ |

现提交组长复验。

---

**返工人签名：** W4  
**返工完成时间：** 2026-09-07 01:10 CST  
**仓库地址：** https://github.com/c8shakei/tea-yulu-grading-platform
