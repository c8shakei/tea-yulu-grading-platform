# 答辩前缺陷修复任务清单

**编制人：** 组长  
**接收人：** PM（转达 W3/W4/W5）  
**日期：** 2026-09-07  
**背景：** W5 M6 已通过，但残留 5 项非阻塞缺陷。为确保答辩演示更稳妥，特拆分任务并指派给各员工修复。

---

## W3 任务（后端）

### BUG-02：后端 HTTPException 未纳入统一响应格式

| 项目 | 内容 |
|---|---|
| **等级** | P3（低风险） |
| **位置** | `src/backend/main.py` 或各路由 |
| **现象** | 重复注册、密码错误等返回 `{detail: "..."}`，而非统一 `{code, message, data}`。 |
| **修复要求** | 在 `main.py` 增加全局异常处理器，捕获 `HTTPException` 并转换为 `{code: status_code, message: detail, data: null}`。 |
| **验证方式** | `python tests/test_backend.py` 全部通过；手动调用错误接口返回统一格式。 |

### BUG-03：注册接口未校验弱密码

| 项目 | 内容 |
|---|---|
| **等级** | P2（中风险） |
| **位置** | `src/backend/auth.py::register` |
| **现象** | 密码为 "1" 也能注册成功。 |
| **修复要求** | 增加密码长度校验，建议 ≥ 6 位；不足时返回 400 及统一错误格式。 |
| **验证方式** | `python tests/test_backend.py` 中弱密码用例通过；W5 测试报告用例覆盖。 |

---

## W4 任务（前端）

### BUG-04：/history 数据列表在手机尺寸下表头换行/截断

| 项目 | 内容 |
|---|---|
| **等级** | P2（中风险） |
| **位置** | `frontend/src/pages/HistoryView.vue` |
| **现象** | iPhone SE/12 Pro 下 `class_name` 显示为 `class_n ame`，`confidence` 显示为 `confide nce`。 |
| **修复要求** | 小屏下使用更短列名（如"等级"、"置信度"）或启用横向滚动，确保列名不换行截断。 |
| **验证方式** | `npm run test:e2e` 通过；响应式检查脚本无表头换行。 |

### BUG-05：个人中心总检测数显示为 0

| 项目 | 内容 |
|---|---|
| **等级** | P2（中风险） |
| **位置** | `frontend/src/pages/ProfileView.vue` |
| **现象** | 登录后已做检测，但 `/profile` 页面"总检测数"显示为 0。 |
| **修复要求** | 进入页面时重新拉取 `GET /api/stats/user/{id}`，确保展示当前用户最新统计。 |
| **验证方式** | E2E 输出中 `[profile]` 页面显示总检测数 ≥ 1。 |

---

## W5 / 组长协调任务

### BUG-06：GitHub 直接 clone 在当前环境失败

| 项目 | 内容 |
|---|---|
| **等级** | P2（中风险） |
| **位置** | 网络环境 / 部署流程 |
| **现象** | 当前环境 `git clone https://github.com/...` 返回 `Recv failure: Connection was reset`。 |
| **修复要求** | 答辩前确认目标网络可访问 GitHub；准备离线部署包（源码 zip + 模型权重 + 依赖脚本）。 |
| **验证方式** | 在答辩用机上能完成 clone 或使用离线包启动。 |

---

## 修复优先级

| 优先级 | 任务 | 原因 |
|---|---|---|
| **P1（建议优先）** | BUG-05 个人中心统计 | 答辩时评委可能点开个人中心，显示 0 会影响观感。 |
| **P1（建议优先）** | BUG-03 弱密码校验 | 基础安全项，修复简单。 |
| P2 | BUG-04 history 手机表格 | 仅影响小屏，答辩用 Laptop 可规避。 |
| P2 | BUG-02 错误格式统一 | 后端代码整洁度，不影响演示。 |
| P2 | BUG-06 离线部署包 | 必须做，但属于部署准备而非代码修复。 |

---

## 验收标准

- W3 修复后：`python tests/test_backend.py` 32 个用例全部通过。
- W4 修复后：`npm run test:e2e` 全量通过，`npm run build` 成功。
- W5/组长：离线部署包可用，答辩环境可启动。
- 全部修复完成后，由组长做最终全链路验证。

---

## 签名栏

| 角色 | 任务 | 状态 | 签名 | 日期 |
|---|---|---|---|---|
| W3 | BUG-02 / BUG-03 | 待修复 | - | - |
| W4 | BUG-04 / BUG-05 | 待修复 | - | - |
| W5 / 组长 | BUG-06 离线包 | 待完成 | - | - |
| 组长 | 最终验证 | 待执行 | - | - |
