# W4 M5 里程碑 — 组长批复

**批复人：** 组长  
**批复对象：** W4（前端） / 项目经理（PM）  
**批复日期：** 2026-09-07  
**里程碑：** M5 — 端到端联调、缺陷修复与构建部署

---

## 一、总体结论

**不直接通过。改为「有条件通过（返工后复验）」。**

W4 M5 在代码层面确实完成了 history Schema 驱动化、baseURL 配置、E2E 可移植化、ECharts 按需引入等目标，`npm run build` 与 `npm run test:e2e` 也能跑通。但本次组长复核发现以下**不可放行的 P0 问题**：

1. **E2E 测试图片不是真实茶叶图片**（`test_tea.jpg` 为 824 字节纯色占位图），无法证明端到端对真实茶叶样本有效。
2. **里程碑汇报文档自身存在事实矛盾**：第 5 节称 GitHub 同步已完成，第 7 节与第 9 节又称 GitHub 阻塞。
3. **GitHub 仓库缺少 README.md**，且已有 `.docx`、`.png`、`.jpg` 等二进制文件入仓，与 `.gitignore` 的排他意图冲突。

作为组长，我不能在「端到端真实性」和「交付文档自洽性」上放水。必须在返工并复验通过后，方可正式批复 M5 完成。

---

## 二、复核发现的具体问题

### P0 阻塞项（必须返工）

| 编号 | 问题 | 根因 | 证据 |
|---|---|---|---|
| P0-1 | E2E 未使用真实茶叶图片 | `frontend/tests/e2e/test_tea.jpg` 为 824 字节纯色占位图（绿色方块） | 文件大小 824 字节；`Read` 预览为纯色图 |
| P0-2 | 里程碑汇报自相矛盾 | 文档由多人/多版本拼接，未做最终一致性校对 | 第 5 节写「GitHub 同步 ✅ 完成」；第 7 节写「⚠️ 阻塞」；第 9 节写「GitHub 同步因权限问题阻塞」 |
| P0-3 | GitHub 仓库缺少 README | 未将 README 纳入 M5-W4-05 交付范围 | `curl .../contents/README.md` 返回 404 |
| P0-4 | 二进制文件已入仓 | 早期提交时 `.gitignore` 未生效或图片被强制添加 | `git ls-tree` 显示 `test_tea.jpg`、`training_curves.png`、`training_curves_yolov8s.png` 在 HEAD 中 |

### P1 高风险项（限期整改或给出可接受说明）

| 编号 | 问题 | 影响 | 要求 |
|---|---|---|---|
| P1-1 | Element Plus 全量引入，chunk 1,088.33 kB | 首屏加载慢，答辩演示可能卡顿 | 给出优化计划（按需引入或构建优化），或在 README 中说明已评估不影响答辩 |
| P1-2 | 训练曲线看板仍为静态 PNG | M5-W4-08 未开始，答辩时无法展示动态训练过程 | 明确计划：若 W2 无法提供 CSV/JSON 接口，则保留静态 PNG 并在 README 中说明 |
| P1-3 | E2E 缺少负向用例 | 仅验证 happy path，未覆盖大文件、非图片、网络异常、未授权访问 | 补充至少 3 个负向用例：超大文件（>10MB）、非图片文件、未登录访问受保护路由 |

### P2 建议项

| 编号 | 问题 | 建议 |
|---|---|---|
| P2-1 | 移动端响应式未做 | 答辩前一周必须完成，建议提前至 M5 收尾阶段 |
| P2-2 | `frontend/.env.production` 为空 | 给出生产部署示例值（如 `/api` 或具体后端域名） |

---

## 三、返工要求与验收标准

W4 必须在 **24 小时内**完成以下返工，并提交组长复验：

1. **替换 E2E 测试图片**
   - 从 `datasets/tea_yulu/test/images/` 中任选一张真实茶叶图片，复制到 `frontend/tests/e2e/test_tea.jpg`。
   - 运行 `npm run test:e2e`，确认检测结果与 `trace_id` 正常生成。
   - 在 `W4_M5_返工说明.md` 中记录图片来源路径与检测输出。

2. **修正里程碑汇报文档**
   - 删除第 7 节、第 9 节中关于 GitHub 阻塞的矛盾描述。
   - 统一口径：GitHub 仓库已创建并推送，当前状态为「已完成」。

3. **补充 GitHub README.md**
   - 必须包含：项目简介、技术栈、目录结构、后端/前端启动方式、环境变量说明、模型权重下载说明、贡献者与许可证。
   - 提交到仓库 `main` 分支。

4. **清理或说明已入仓二进制文件**
   - 优先方案：从 Git 历史中移除 `*.png`、`*.jpg`、`*.docx`（可用 `git filter-repo` 或 `git rm --cached` + 重写历史）。
   - 降级方案：若无法重写历史，必须在 README 中明确列出这些文件的用途，并说明为何保留在仓库中。

5. **补充 E2E 负向用例**
   - 至少增加 3 个负向用例，并在 `tests/e2e/manual_check.cjs` 中实现。
   - 运行 `npm run test:e2e` 全量通过。

6. **更新返工说明文档**
   - 新建 `W4_M5_返工说明.md`，逐项说明问题、修复方式、验证结果。

---

## 四、通过条件（必须全部满足）

1. P0-1 ~ P0-4 全部修复，组长抽查通过。
2. `npm run build` 成功，无新增构建错误。
3. `npm run test:e2e` 在真实茶叶图片上跑通，且新增负向用例通过。
4. GitHub 仓库可见 README.md，且 `.docx`/`.png`/`.jpg` 已移除或已在 README 中说明。
5. W5 全量接口/E2E 测试通过（由 W5 在返工完成后介入）。

---

## 五、组长裁定

- **M5 不直接通过，改为「有条件通过（返工后复验）」。**
- W4 在返工期间仍归属 M5 阶段，不进入答辩前收尾阶段。
- 训练曲线动态化（M5-W4-08）与移动端响应式（M5-W4-09）不阻塞 M5，但必须在答辩前完成。
- 项目经理（PM）负责监督 W4 在 24 小时内提交返工材料。

---

**批复人签名：** 组长  
**批复时间：** 2026-09-07 00:46 CST  
**下次复验时间：** 2026-09-08 00:46 CST 前
