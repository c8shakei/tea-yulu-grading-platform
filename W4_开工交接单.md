# W4 开工交接单

> 交接时间：2026-09-06  
> 交接人：项目经理助理  
> 接收人：W4（前端工程师）  
> 前置里程碑：W3 M3 后端与溯源已通过审查并修复全部阻塞项

---

## 一、W3 M3 交付状态（已确认可用）

| 检查项 | 状态 | 说明 |
|---|---|---|
| 后端服务可启动 | ✅ | `scripts/start_backend.py` → `http://0.0.0.0:8000` |
| 后端自测全通过 | ✅ | PowerShell 下 `tests/test_backend.py` 14 项检查通过 |
| 接口契约③完整 | ✅ | 11 个接口全部实现 |
| 哈希链验证 | ✅ | 篡改检测可用 |
| 认证与权限 | ✅ | JWT + 所有者/管理员校验 |
| 上传大小限制 | ✅ | 默认 10MB |
| UI Schema 可读取 | ✅ | `GET /api/ui/schema` 返回完整配置 |

**最终结论**：W3 M3 后端通过，W4 可立即开工。

---

## 二、W3 接口清单（W4 必须对接）

| 方法 | 路径 | 用途 | 认证 |
|---|---|---|---|
| POST | /api/auth/register | 用户注册 | 否 |
| POST | /api/auth/login | 用户登录（OAuth2 form） | 否 |
| GET  | /api/auth/me | 获取当前用户信息 | 是 |
| POST | /api/auth/logout | 登出 | 是 |
| POST | /api/detect | 图片检测分级 | 可选（登录后记录 user_id） |
| POST | /api/trace | 追加溯源区块 | 是（所有者/管理员） |
| GET  | /api/trace/{id} | 查询并验证哈希链 | 否 |
| GET  | /api/detections | 当前用户检测历史 | 是 |
| GET  | /api/ui/schema | UI 低代码配置 | 否 |
| GET  | /api/stats/overview | 全局统计（首页仪表盘） | 否 |
| GET  | /api/stats/user/{user_id} | 用户统计（历史/个人中心） | 是 |
| GET  | /models/{filename} | W2 静态产物（训练曲线、指标 JSON） | 否 |

统一响应格式：`{"code": 0, "message": "ok", "data": {...}}`，业务数据在 `data` 字段。

---

## 三、页面清单与路由（必须实现）

| 路由 | 页面名称 | 数据来源 | 关键功能 |
|---|---|---|---|
| /login | 登录页 | POST /api/auth/login | 登录后存 token 跳转首页 |
| /register | 注册页 | POST /api/auth/register | 注册成功后跳转 /login |
| / | 首页仪表盘 | GET /api/stats/overview | 统计卡片 + 等级饼图 + 近7天趋势折线图 |
| /detect | 检测工作台 | POST /api/detect | 上传图片、显示分级结果、可追加复核 |
| /history | 历史记录 | GET /api/detections<br>GET /api/stats/user/{user_id} | 表格 + 等级饼图 + 置信度趋势折线图 |
| /trace | 溯源验证 | GET /api/trace/{id} | 输入 trace_id，显示哈希链时间线，篡改标红 |
| /profile | 个人中心 | GET /api/auth/me<br>GET /api/stats/user/{user_id} | 用户信息 + 登出 + 个人统计图表 |
| /lowcode | 低代码演示 | GET /api/ui/schema | JSON 编辑器，修改后应用可实时改变导航/页面 |
| /training | 训练曲线看板 | GET /models/training_curves.png<br>GET /models/test_metrics.json | 展示 W2 训练曲线与指标 |

说明：导航由 `GET /api/ui/schema` 的 `nav` 字段自动生成；`login`/`register` 不在导航中。

---

## 四、UI Schema 结构（W3 已交付，按此渲染）

运行时从 `GET /api/ui/schema` 获取，结构如下：

```json
{
  "appName": "恩施玉露茶品质分级与溯源平台",
  "version": "0.3.0",
  "theme": {
    "primary": "#2E7D32",
    "secondary": "#81C784",
    "background": "#F5F5F5",
    "text": "#212121"
  },
  "nav": [
    { "key": "dashboard", "label": "首页仪表盘", "icon": "DashboardOutlined", "path": "/" },
    { "key": "detect", "label": "检测工作台", "icon": "CameraOutlined", "path": "/detect" },
    { "key": "history", "label": "历史记录", "icon": "HistoryOutlined", "path": "/history" },
    { "key": "trace", "label": "溯源验证", "icon": "SafetyCertificateOutlined", "path": "/trace" },
    { "key": "profile", "label": "个人中心", "icon": "UserOutlined", "path": "/profile" },
    { "key": "lowcode", "label": "低代码演示", "icon": "CodeOutlined", "path": "/lowcode" },
    { "key": "training", "label": "训练曲线看板", "icon": "LineChartOutlined", "path": "/training" }
  ],
  "pages": {
    "dashboard": { "title": "首页仪表盘", "widgets": [...] },
    "detect": { "title": "检测工作台", "widgets": [...] },
    "history": { "title": "历史记录", "widgets": [...] },
    "trace": { "title": "溯源验证", "widgets": [...] },
    "profile": { "title": "个人中心", "widgets": [...] },
    "lowcode": { "title": "低代码演示", "widgets": [...] },
    "training": { "title": "训练曲线看板", "widgets": [...] }
  }
}
```

widgets 中 `type` 字段决定渲染组件，W4 必须支持：
- `stat-card`、`pie-chart`、`line-chart`
- `image-upload`、`result-card`
- `data-table`、`trace-input`、`trace-chain`
- `user-info`、`json-viewer`
- `image`、`metrics`

---

## 五、W2 产物（训练曲线看板用）

| 文件 | 路径 | 用途 |
|---|---|---|
| 训练曲线图 | /models/training_curves.png | /training 页面直接展示 |
| 测试指标 JSON | /models/test_metrics.json | /training 页面解析展示 |
| 训练结果 CSV | /models/results.csv | 可选展示 |
| yolov8s 训练曲线 | /models/training_curves_yolov8s.png | 可选 |
| yolov8s 测试指标 | /models/test_metrics_yolov8s.json | 可选 |

---

## 六、组件与目录建议

```
frontend/
├── package.json
├── vite.config.js
├── index.html
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── router/index.js          # 路由表 + 登录守卫
│   ├── api/
│   │   ├── auth.js
│   │   ├── detect.js
│   │   ├── trace.js
│   │   ├── stats.js
│   │   └── schema.js
│   ├── stores/
│   │   └── user.js              # Pinia 用户状态
│   ├── layouts/
│   │   └── MainLayout.vue       # 导航 + 内容区
│   ├── pages/
│   │   ├── LoginView.vue
│   │   ├── RegisterView.vue
│   │   ├── DashboardView.vue
│   │   ├── DetectView.vue
│   │   ├── HistoryView.vue
│   │   ├── TraceView.vue
│   │   ├── ProfileView.vue
│   │   ├── LowcodeView.vue
│   │   └── TrainingView.vue
│   ├── components/
│   │   ├── SchemaRender.vue     # 根据 pages[key].widgets 渲染
│   │   ├── NavMenu.vue
│   │   └── charts/
│   │       ├── StatCards.vue
│   │       ├── GradePieChart.vue
│   │       ├── TrendLineChart.vue
│   │       ├── ConfidenceLineChart.vue
│   │       ├── HashChainTimeline.vue
│   │       └── TrainingCurvePanel.vue
│   └── utils/
│       ├── request.js           # axios 实例 + 拦截器
│       └── auth.js              # token 读写
```

---

## 七、环境要求与启动方式

1. 后端必须先启动：
   ```powershell
   cd C:/Users/wzd/Desktop/毕业设计
   C:/Users/wzd/.workbuddy/binaries/python/envs/tea-backend/Scripts/python.exe scripts/start_backend.py
   ```

2. 前端启动：
   ```bash
   cd frontend
   npm install
   npm run dev
   ```

3. 联调地址：
   - 后端：`http://127.0.0.1:8000`
   - 前端：`http://127.0.0.1:5173`（Vite 默认）

---

## 八、自测清单（W4 提交前必须全部通过）

- [ ] `npm install` 成功
- [ ] `npm run dev` 成功，无编译错误
- [ ] /login 可登录，登录后导航显示个人中心/历史记录/检测工作台
- [ ] /register 可注册，注册成功后跳转 /login
- [ ] / 显示统计卡片、等级饼图、近7天趋势折线图
- [ ] /detect 可上传图片并显示 class_name / confidence / trace_id
- [ ] /history 显示表格、等级饼图、置信度趋势折线图
- [ ] /trace 输入 trace_id 后显示时间线，valid=false 时标红
- [ ] /profile 显示当前用户名和登出按钮
- [ ] /lowcode 显示 JSON 编辑器，修改后点击应用可改变导航/页面标题
- [ ] /training 显示训练曲线图片和指标 JSON
- [ ] 未登录访问 /history 重定向到 /login
- [ ] 刷新页面后仍保持登录态（localStorage 读取 token）

---

## 九、阻塞上报口径

W4 在开发中遇到以下情况必须立即上报组长，不得自行改 W3 接口或 W2 产物：

1. 后端接口返回字段与契约不一致（如 `stats/overview` 缺 `grade_distribution`）。
2. `GET /api/ui/schema` 返回的 nav/pages 结构与 W4 提示词不一致。
3. `/models/training_curves.png` 或 `/models/test_metrics.json` 无法访问。
4. `/api/detect` 上传图片失败或返回格式异常。
5. 需要新增接口才能继续前端开发。
