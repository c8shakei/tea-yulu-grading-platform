# W4 M4 里程碑汇报：前端工程与联调自测

**汇报人：** W4（前端）  
**汇报对象：** 组长（请批复） / 项目经理（PM，请转交）  
**汇报时间：** 2026-09-06  
**里程碑：** M4 — 前端界面实现与前后端联调  

---

## 1. 目标回顾

完成 Vue 3 + Vite 前端工程搭建，实现 UI Schema 驱动的页面渲染、9 个业务页面、ECharts 可视化组件，并与 W3 后端完成联调自测。

---

## 2. 已完成工作清单

| 模块 | 内容 | 状态 | 备注 |
|------|------|------|------|
| 工程初始化 | Vue 3 + Vite + Element Plus + Pinia + Vue Router + axios | ✅ 完成 | `frontend/` 目录 |
| 网络与状态 | axios 拦截器、token 读写、Pinia user store | ✅ 完成 | `src/utils/`、`src/stores/` |
| API 层 | auth / detect / trace / stats / schema 模块 | ✅ 完成 | `src/api/` |
| 路由与布局 | Vue Router 路由表、登录守卫、MainLayout、NavMenu | ✅ 完成 | `src/router/`、`src/layouts/` |
| SchemaRender 引擎 | 根据 `/api/ui/schema` 的 `pages[key].widgets` 自动渲染组件 | ✅ 完成 | `src/components/SchemaRender.vue` |
| 业务页面 | login / register / dashboard / detect / history / trace / profile / lowcode / training | ✅ 完成 | `src/pages/` |
| ECharts 组件 | StatCards、GradePieChart、TrendLineChart、ConfidenceLineChart、HashChainTimeline、TrainingCurvePanel | ✅ 完成 | `src/components/charts/` |
| 后端联调 | 已启动 W3 后端并调用注册/登录/统计接口 | ✅ 完成 | 端口 8000 |
| E2E 自测 | 使用 Playwright 完成 11 个场景截图与基础巡检 | ✅ 通过 | 见 `tests/e2e/screenshots/` |

---

## 3. 测试结果

- **后端服务：** `http://127.0.0.1:8000` 正常启动，`/api/ui/schema`、`/api/auth/register`、`/api/auth/login`、`/api/stats/overview` 等接口可调用。
- **前端服务：** `http://localhost:5173` 正常启动，热更新正常。
- **E2E 巡检脚本：** `frontend/tests/e2e/manual_check.cjs` 全部通过，生成 11 张截图：
  - `01_dashboard_anon.png`
  - `02_history_redirect_to_login.png`
  - `03_register_after_click.png` / `03_register_success.png`
  - `04_login_success.png`
  - `05_dashboard_loggedin.png`
  - `06_detect.png`
  - `07_history.png`
  - `08_trace.png`
  - `09_profile.png`
  - `10_lowcode.png`
  - `11_training.png`

**测试结论：** 注册 → 登录 → 各页面跳转 → 图表渲染 → 登出 主链路可跑通。

---

## 4. 关键技术与实现要点

| 项 | 说明 |
|----|------|
| Schema 驱动 | `MainLayout` 调用 `/api/ui/schema` 后通过 `provide/inject` 把 schema 下发给各页面，`SchemaRender` 根据 `widget.type` 映射到具体组件。 |
| 低代码演示 | `LowcodeView` 提供 JSON 编辑器，可实时修改 schema 并预览页面，便于 W3 后端调整 UI 配置。 |
| history 页面增强 | W3 后端 schema 中 history 页仅含 `data-table`；W4 在前端对内存 schema 做了补丁，补充了 `grade_distribution` 饼图与 `confidence_trend` 折线图。 |
| ECharts | 使用原生 `echarts` 按需引入与全量引入双模式；图表组件统一接收 `data`、`title` 与 `metric` props。 |
| 路由守卫 | 未登录用户访问 `/history`、`/detect`、`/profile` 等会被重定向到 `/login`。 |

---

## 5. 阻塞风险

| 风险 | 影响 | 当前状态 | 建议 |
|------|------|----------|------|
| W3 后端 schema 中 history 页缺少图表配置 | 若删除前端补丁，history 页仅展示表格 | 已通过前端补丁缓解 | 建议 W3 在 `ui_schema.json` 的 `history.widgets` 中补全 `pie-chart` 与 `line-chart` 配置，或保留前端补丁作为永久方案 |
| detect 页实际模型推理依赖 W2 模型与 W3 `/api/detect` 接口 | E2E 中仅上传图片并查看空结果，未触发真实推理 | 功能入口已通 | W2/W3 完成模型接入后需再次端到端验证 |
| training 页数据来源为 mock 数据 | 图表可渲染，但未接入真实训练日志 | 占位可用 | 建议 W2 提供训练曲线 API 后替换 |
| trace 页未生成真实 trace_id | 仅验证页面渲染与查询表单 | 入口已通 | 待 detect 产生真实上链记录后验证完整溯源链路 |
| 图片上传 / 检测结果展示依赖后端响应格式 | 当前前端按 W3 接口文档实现 | 待确认 | 建议 W3/W4 联调时确认 `POST /api/detect` 返回字段 |

---

## 6. 下一步（M4 后）

1. **等待组长批复** M4 里程碑后，W4 进入 M5 缺陷修复与性能优化阶段。
2. **与 W3 联调 detect 完整链路**：上传图片 → 后端推理 → 返回等级/置信度/图片 → 前端展示。
3. **与 W2 对接训练曲线 API**：替换 `TrainingCurvePanel` 中的 mock 数据。
4. **UI 走查**：根据 PM/组长反馈调整布局、颜色、响应式。
5. **打包构建**：验证 `npm run build` 产物，准备部署。

---

## 7. 待决策项

| 编号 | 事项 | 建议方案 | 决策人 |
|------|------|----------|--------|
| D1 | history 页图表配置是否应由后端 schema 提供？ | 由 W3 在 `ui_schema.json` 中补全，W4 移除前端补丁 | 组长 |
| D2 | training 页是否接入真实训练曲线？ | 是，待 W2 提供 API 后接入 | 组长 |
| D3 | 前端是否需要在本次继续完善响应式移动端适配？ | M4 以桌面端为主，M5 统一处理 | 组长/PM |
| D4 | 是否将前端项目同步到 GitHub 仓库？ | 是，建议 M4 结束后统一 push | 组长/PM |

---

## 8. 审批申请

W4 已完成 M4 前端工程搭建、页面实现与前后端联调自测，现申请组长批复进入下一阶段。

- **申请人：** W4
- **申请时间：** 2026-09-06
- **批复人：** 组长（请批复）
