# W5 M6 答辩前测试结论

**报告人：** W5（测试与集成工程师）  
**接收人：** 组长  
**日期：** 2026-09-07  
**阶段：** M6 — 答辩前全量测试与集成收尾  

---

## 一、验收标准对照

| 验收条件 | 要求 | 实际 | 结论 |
|---|---|---|---|
| 1. 后端接口测试 | ≥ 30 个用例，通过率 ≥ 95% | 32 个用例，通过率 100% | **通过** |
| 2. 前端 E2E 可复现性 | 干净 clone 目录跑通 | 已通过（使用本地源 clone） | **通过** |
| 3. 真实茶叶图片端到端 | 链路跑通 | 特级 / 73.61%，trace_id 与哈希链验证通过 | **通过** |
| 4. 跨用户数据隔离 | 验证通过 | `GET /api/detections`、`GET /api/stats/user/{id}` 已隔离 | **通过** |
| 5. 缺陷清单 P0/P1 | ≤ 3 个或已全部指派 | 共 0 个未修复 P0/P1（BUG-01 已修复并验证） | **通过** |
| 6. 答辩演示就绪 | 明确结论 | 当前版本**可答辩演示** | **通过** |

---

## 二、演示就绪状态

**结论：当前版本可答辩演示。**

### 可演示链路

1. **注册 / 登录 / 登出**
2. **上传真实茶叶图片** → 返回等级（特级）、置信度（73.61%）、trace_id
3. **历史记录** → 个人检测记录列表与统计图表
4. **溯源验证** → 输入 trace_id → 显示完整哈希链 →「哈希链验证通过」
5. **统计看板** → 首页总检测数、等级分布、近 7 天趋势
6. **训练曲线看板** → W2 训练曲线与测试指标
7. **低代码演示** → `/lowcode` 展示 UI Schema

### 答辩脚本建议

```text
1. 打开浏览器访问 http://localhost:5173
2. 展示首页仪表盘（未登录也可访问）
3. 点击「检测工作台」→ 系统提示登录
4. 注册新账号并登录
5. 进入「检测工作台」→ 上传 datasets/tea_yulu/test/images/ 中的真实茶叶图片
6. 展示检测结果：特级 / 73.61%
7. 复制 trace_id → 进入「溯源验证」→ 查询并展示哈希链
8. 进入「历史记录」→ 展示个人检测记录
9. 进入「训练曲线看板」→ 展示 W2 训练成果
10. 登出
```

---

## 三、遗留风险

| 风险编号 | 风险描述 | 影响 | 建议处理方式 |
|---|---|---|---|
| R1 | `/api/trace/{id}` 读取权限 | 已修复 | 已增加 owner/admin 校验，测试通过 |
| R2 | GitHub 直接 clone 在当前环境失败 | 中 | 答辩前确认网络；准备本地/镜像备用方案 |
| R3 | `/history` 手机尺寸表头换行/截断 | 低 | 建议 W4 优化，非阻塞 |
| R4 | 个人中心总检测数显示为 0 | 低 | 建议 W4 修复数据刷新逻辑，非阻塞 |
| R5 | 后端错误格式不统一（`detail` vs `message`） | 低 | 建议 W3 统一，非阻塞 |
| R6 | UI Schema 页面数量口径 | 已确认 | 9 个页面 = 7 个 schema 业务页面 + login/register 硬编码页面 |

---

## 四、必须修复项（答辩前）

当前已无必须修复的 P0/P1 缺陷。

### 建议修复项（非阻塞）

1. **BUG-04：/history 手机表格可读性**（P2，W4）
2. **BUG-05：个人中心统计刷新**（P2，W4）
3. **BUG-02：错误响应格式统一**（P3，W3）
4. **BUG-03：弱密码校验**（P2，W3）
5. **BUG-06：GitHub 直连网络**（P2，W5 / 组长协调）

---

## 五、最终结论

| 项目 | 结论 |
|---|---|
| **M6 是否通过** | **通过**（满足全部 6 条验收标准，BUG-01 已修复并验证） |
| **是否可答辩演示** | **可以** |
| **遗留风险数量** | 5 项，均为建议优化或非阻塞风险 |
| **是否建议进入答辩准备** | **建议进入最终彩排**；其他问题可在答辩演示中规避（如使用 Laptop 尺寸、不主动展示个人中心统计） |

---

## 六、已决策项（组长批复）

1. **BUG-01 修复方案**：已同意并已由组长现场修复，验证通过。
2. **BUG-07 口径**：9 个页面 = 7 个 schema 业务页面 + `/login` + `/register`，两者不矛盾。
3. **GitHub 网络**：答辩前由 PM/组长确认目标网络环境，并准备离线部署包备用。
