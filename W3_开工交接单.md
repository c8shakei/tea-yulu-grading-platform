# W3 开工交接单

> 交接时间：2026-09-05  
> 交接人：W2 视觉模型工程师 → W3 后端与溯源工程师  
> 审批：组长已批复 W2 M2 完成，W3 可立即开工。

---

## 一、项目背景（冻结约束，W3 不可更改）

- **项目名**：基于计算机视觉的农产品品质分级与溯源平台
- **应用作物**：恩施玉露茶（湖北地理标志绿茶）
- **数据集**：TeaLeafAgeQuality（Mendeley，DOI: 10.17632/7t964jmmy3，CC BY 4.0）
- **技术栈**：YOLOv8 + FastAPI + SQLite + Python hashlib + Vue + ECharts
- **类别映射（必须严格一致）**：
  - `0` → 特级
  - `1` → 一级
  - `2` → 二级
  - `3` → 等外
- **工期**：30 天硬基线，当前处于 M2→M3 过渡。

---

## 二、W2 已交付产物（可直接使用）

| 产物 | 路径 | 说明 |
|---|---|---|
| 检测主模型权重 | `models/best_detect.pt` | **yolov8n，当前主模型，W3 必须加载** |
| 分类对照权重 | `models/best_cls.pt` | yolov8n-cls，论文对照用 |
| 消融检测权重 | `models/best_detect_yolov8s.pt` | yolov8s 消融，W3 不加载 |
| 消融分类权重 | `models/best_cls_yolov8s-cls.pt` | yolov8s-cls 消融，W3 不加载 |
| 检测指标 | `models/test_metrics.json` | mAP50-95=0.702，供统计看板读取 |
| 分类指标 | `models/val_metrics.json` | Top-1=0.904，供统计看板读取 |
| 训练曲线 | `models/training_curves.png` | 前端训练曲线看板素材 |
| 推理封装 | `src/inference/detector.py` | W3 直接调用 |

### W2 主模型指标

| 模型 | mAP50-95 | mAP50 | Precision | Recall |
|---|---|---|---|---|
| yolov8n（主模型） | 0.702 | 0.880 | 0.799 | 0.867 |
| yolov8s（消融） | 0.743 | 0.906 | 0.884 | 0.851 |

### 组长决策（W3 必须遵守）

1. **yolov8n 继续作为主模型**，W3 加载 `models/best_detect.pt`。
2. **yolov8s 暂不升级**，仅作为论文消融对照，W3 不需要切换。
3. 检测 mAP50-95 目标 0.90 尚未达成，后续通过 W3/W4 的数据增强、后处理、阈值调优补偿，不阻塞当前开工。

---

## 三、W3 核心任务

用 **FastAPI + SQLite + hashlib** 实现后端与溯源模块，暴露 RESTful API 供 W4 前端调用。

### 3.1 必做接口（契约③）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录，返回 JWT |
| GET | `/api/auth/me` | 获取当前登录用户信息 |
| POST | `/api/auth/logout` | 登出（客户端清除 token 即可） |
| POST | `/api/detect` | 图片检测，返回等级、置信度、Bbox、trace_id |
| POST | `/api/trace` | 将检测数据上链，返回区块信息 |
| GET | `/api/trace/{id}` | 查询某条检测的哈希链验证结果 |
| GET | `/api/detections` | 当前用户的历史检测记录 |
| GET | `/api/ui/schema` | 返回低代码页面配置 JSON |
| GET | `/api/stats/overview` | 全站统计（总检测数、今日、等级分布、7 天趋势） |
| GET | `/api/stats/user/{user_id}` | 用户维统计（总数、等级分布、置信度趋势） |

统一响应格式：

```json
{
  "code": 0,
  "message": "ok",
  "data": { ... }
}
```

### 3.2 数据库 Schema（建议）

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    password_hash TEXT NOT NULL,
    role TEXT DEFAULT 'user',
    created_at TEXT NOT NULL
);

CREATE TABLE detections (
    id TEXT PRIMARY KEY,
    user_id INTEGER REFERENCES users(id) ON DELETE SET NULL,
    image_hash TEXT,
    class_id INTEGER,
    class_name TEXT,
    confidence REAL,
    bbox TEXT,
    trace_block_index INTEGER REFERENCES hashchain(index),
    created_at TEXT
);

CREATE TABLE hashchain (
    index INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp REAL,
    prev_hash TEXT,
    data_hash TEXT,
    hash TEXT,
    payload TEXT
);
```

### 3.3 哈希链契约（必须实现）

每个区块：

```json
{
  "index": 0,
  "timestamp": 1693900000.123,
  "prev_hash": "0000...0000",
  "data_hash": "sha256(json(payload))",
  "hash": "sha256(index + timestamp + prev_hash + data_hash)",
  "payload": "{...}"
}
```

- 创世区块 `prev_hash = "0" * 64`
- 必须提供 `/api/trace/{id}` 验证整条链未被篡改
- 验证逻辑：重新计算每个区块的 hash，与存储值比对

### 3.4 与 W2 推理封装集成

W3 在 `/api/detect` 中这样调用：

```python
from src.inference.detector import TeaGradeDetector

# 启动时加载一次，不要每次请求都重新加载
detector = TeaGradeDetector(weight_path="models/best_detect.pt", device="cpu")

@app.post("/api/detect")
def detect_image(file: UploadFile):
    result = detector.detect(file.file)  # 支持文件对象/路径/numpy
    # result = {class_id, class_name, confidence, bbox}
    # 生成 trace_id，写入 detections 表，并上链
    return {"code": 0, "message": "ok", "data": result}
```

注意：
- `TeaGradeDetector` 已经做了 CPU 推理设置，`device="cpu"` 即可。
- 类别映射 W2 已保证，W3 直接透传。
- 不要每次请求重新 `YOLO()`，否则推理极慢。

### 3.5 UI Schema（CR-W1-004）

- 文件位置固定：`src/backend/ui_schema.json`
- 接口：`GET /api/ui/schema` 直接读取该 JSON 返回
- Schema 至少包含：`appName`、`nav`、`pages`
- 示例页面：首页仪表盘、检测工作台、历史记录、溯源验证、个人中心、低代码演示、训练曲线看板

### 3.6 统计接口（CR-W1-005）

- 只做只读聚合，**禁止新增业务表，禁止改 detections schema**
- `GET /api/stats/overview` 返回：
  - `total_detections`
  - `today_detections`
  - `grade_distribution`：饼图数据 `[{name:"特级", value:10}, ...]`
  - `weekly_trend`：近 7 天检测数量折线图 `[{date:"2026-09-01", count:5}, ...]`
- `GET /api/stats/user/{user_id}` 返回：
  - `total_detections`
  - `grade_distribution`
  - `confidence_trend`：平均置信度趋势

---

## 四、目录结构要求

```
C:/Users/wzd/Desktop/毕业设计/
├── src/
│   ├── backend/              # W3 后端代码根目录
│   │   ├── main.py           # FastAPI 入口
│   │   ├── auth.py           # JWT 认证
│   │   ├── database.py       # SQLite 连接与表初始化
│   │   ├── detections.py     # 检测/历史/统计接口
│   │   ├── traceability.py   # 哈希链模块
│   │   ├── ui_schema.json    # 低代码配置（静态 JSON）
│   │   └── config.py         # 环境变量/配置
│   └── inference/
│       └── detector.py       # W2 产物，W3 只读调用
├── models/
│   └── best_detect.pt        # W2 主模型权重
├── tests/                    # W5 测试用例（W3 可先写基础自测）
└── docs/
    └── W3_M3_接口说明.md     # W3 交付文档
```

---

## 五、W3 交付物清单

完成 M3 时必须提交：

1. `src/backend/` 完整后端代码（FastAPI + SQLite + hashchain + auth + stats）
2. `src/backend/ui_schema.json` 低代码配置文件
3. `docs/W3_M3_接口说明.md`：接口清单、字段说明、认证流程、数据库 ERD
4. `docs/W3_M3_哈希链验证说明.md`：不可篡改验证流程
5. 可运行的 OpenAPI 文档（启动后访问 `/docs`）
6. 自测脚本或 Postman/HTTP 请求示例，覆盖：
   - 注册 / 登录
   - 检测（匿名与登录态）
   - 上链 / 溯源验证
   - 历史记录查询
   - 统计接口

---

## 六、质量要求

- 密码必须哈希存储（推荐 `passlib[bcrypt]` 或 `werkzeug`）
- JWT secret 从环境变量读取，本地开发可用默认值
- 所有接口必须返回统一响应格式 `{code, message, data}`
- 哈希链必须能跑通不可篡改验证（篡改任一字段后验证失败）
- SQLite 用单文件，便于演示
- 代码需有注释，变量命名清晰

---

## 七、里程碑与工期

- **M3 目标**：后端可用（D16 左右）
- **验收标准**：W4 能对接 `/api/detect`、`/api/auth/login`、`/api/stats/overview`、`/api/ui/schema`
- **阻塞上报**：如果遇到 W2 推理封装调用异常、接口字段与 W4 冲突、数据库设计无法支撑统计需求，立即上报组长。

---

## 八、附：W3 可立即验证的环境检查

启动前请确认：

1. Python 3.10+ 已安装
2. `pip install fastapi uvicorn python-multipart python-jose passlib[bcrypt] ultralytics`（W2 环境已覆盖部分依赖）
3. `models/best_detect.pt` 存在
4. `src/inference/detector.py` 可正常导入

```bash
# 快速验证 W2 推理封装
python -c "from src.inference.detector import detect; print(detect('datasets/tea_yulu/test/images/任意图片.jpg'))"
```
