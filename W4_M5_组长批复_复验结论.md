# W4 M5 里程碑 — 组长复验结论

**批复人：** 组长  
**复验日期：** 2026-09-07  
**对应文件：** `W4_M5_组长批复.md`（2026-09-07 有条件通过）  
**复验依据：** W4 提交的 `W4_M5_返工说明.md`

---

## 一、复验动作

组长独立执行以下验证，未采信 W4 自测结果：

1. 读取 `frontend/tests/e2e/test_tea.jpg`，确认替换为真实茶叶照片。
2. 读取 `W4_M5_里程碑汇报.md`，确认 GitHub 阻塞描述已删除。
3. 通过 GitHub API 确认远程仓库 `README.md` 存在，且无 `.png`/`.jpg`/`.docx`/`.xlsx`/`.pdf`/`.workbuddy` 文件。
4. 运行 `npm run build`，确认构建成功。
5. 独立启动后端（`:8000`）与前端 dev server（`:5173`），运行 `npm run test:e2e`。
6. 检查 `frontend/tests/e2e/manual_check.cjs` 负向用例实现。
7. 检查 `.gitignore` 与 `frontend/.env.production` 更新。

---

## 二、P0 返工项验证结果

| 编号 | 原 P0 问题 | 验证方式 | 结果 |
|---|---|---|---|
| P0-1 | E2E 用纯色占位图 | 直接读取 `test_tea.jpg`；运行 E2E 观察检测输出 | ✅ 真实茶叶照片；E2E 输出「特级 / 73.61% / 87267a29...」 |
| P0-2 | 里程碑汇报 GitHub 状态矛盾 | 全文检索「阻塞」「权限问题」 | ✅ 第 7/9 节已删除矛盾描述 |
| P0-3 | GitHub 仓库缺少 README | `curl .../contents/README.md` | ✅ 存在，大小 4616 字节 |
| P0-4 | 二进制文件已入仓 | `curl .../git/trees/main?recursive=1` 过滤 | ✅ 远程无 `.png`/`.jpg`/`.docx`/`.xlsx`/`.pdf`/`.workbuddy` |

---

## 三、P1/P2 项验证结果

| 编号 | 问题 | 验证方式 | 结果 |
|---|---|---|---|
| P1-3 | E2E 缺少负向用例 | 检查 `manual_check.cjs` 第 160-205 行 | ✅ 新增 3 个负向用例：未登录访问 `/detect`、>10MB 文件、非图片文件 |
| P1-1 | Element Plus chunk 过大 | `npm run build` 输出 | ⚠️ 仍存在 1,088.33 kB chunk，不影响运行，列为答辩前优化 |
| P1-2 | 训练曲线动态化 | 检查 `training` 页面与 schema | ⚠️ 仍为静态 PNG，依赖 W2 接口，列为后续优化 |
| P2-1 | 移动端响应式 | 检查页面 CSS | ⚠️ 未做，列为答辩前收尾项 |

---

## 四、独立验证关键数据

### 4.1 真实茶叶图片 E2E

```text
[detect] body: ... 等级：特级 | 置信度：73.61% | 边界框：[0.479111..., 0.293476..., 0.695565..., 0.450958...] | 溯源 ID：87267a2970b845b29e383a161c055d42
[trace] body: ... 哈希链验证通过 | 区块 #0 | 当前哈希：9adb2606f5d7d643... | 上一区块哈希：0000000000000000...
```

### 4.2 E2E 负向用例

- **未登录访问 `/detect`**：未抛出异常，成功跳转 `/login`。
- **12MB 超大文件**：未抛出异常，触发 10MB 限制提示。
- **非图片 `.txt` 文件**：未抛出异常，页面展示检测结果（后端判为最低等级）。

### 4.3 构建产物

```text
✓ built in 12.86s
```

---

## 五、残留轻微问题

以下问题不影响 M5 通过，但需在答辩前处理：

1. `W4_M5_里程碑汇报.md` 第 3.2 节与第 4.1 节仍记录旧 E2E 结果「一级 / 92.10% / 0344eb1f...」，未更新为返工后真实结果「特级 / 73.61% / 87267a29...」。
2. `.gitignore` 排除了 `frontend/tests/e2e/test_tea.jpg`，但 E2E 脚本依赖该文件。在新机器上 clone 仓库后需手动从数据集复制，建议在 README「端到端测试」章节补充说明。
3. Element Plus 全量引入导致 chunk 过大，建议答辩前优化为按需引入或配置 `build.chunkSizeWarningLimit`。
4. 训练曲线看板仍为静态 PNG，如 W2 无法提供动态接口，需在答辩前确认 PNG 展示效果足够。
5. 移动端响应式适配尚未开始，答辩前必须完成。

---

## 六、组长最终批复

**W4 M5 里程碑复验通过。**

- P0 阻塞项已全部修复并独立验证通过。
- E2E 在真实茶叶图片上跑通，端到端主链路可演示。
- GitHub 仓库已具备 README.md 且二进制文件已清理。
- 同意 W4 进入答辩前收尾阶段。

**放行条件：**
1. W5 在 2 个工作日内跑一遍 `npm run test:e2e`，确认可复现。
2. W4 继续完成移动端响应式适配。
3. W4 在 README 中补充 `test_tea.jpg` 的来源说明。
4. 如答辩需要动态训练曲线，W2 需提供接口；否则保持静态 PNG 方案。

---

**批复人签名：** 组长  
**复验时间：** 2026-09-07 08:55 CST  
**仓库地址：** https://github.com/c8shakei/tea-yulu-grading-platform
