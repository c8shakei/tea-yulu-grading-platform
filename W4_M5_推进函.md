# W4 M5 阶段任务推进函

> **文件编号**：M5-W4-001  
> **发函人**：项目经理（PM）  
> **收函人**：W4（前端工程师）  
> **抄送**：组长（请批复）、W3（后端）、W5（测试集成）  
> **发函时间**：2026-09-06  
> **前置里程碑**：W4 M4 前端界面实现与前后端联调已通过审查  
> **阶段目标**：M5 — 端到端联调、缺陷修复、构建部署与项目同步

---

## 一、M5 阶段目标

在 M4 已实现 9 个页面、SchemaRender 引擎、ECharts 组件与基础联调的前提下，M5 聚焦以下目标：

1. **端到端主链路跑通**：注册 → 登录 → 上传真实茶叶图片 → 后端推理 → 前端展示分级结果与 trace_id → 历史记录可查 → 溯源验证可展示哈希链。
2. **修复 M4 审查问题**：history 页 Schema 驱动化、E2E 脚本可移植、生产环境 baseURL 配置、chunk 优化。
3. **构建与部署准备**：验证 `npm run build` 产物，配置生产环境变量，输出部署说明。
4. **项目同步到 GitHub**：将整个毕业设计项目（含 frontend、backend、docs、models 等）推送到 GitHub 仓库。
5. **移动端响应式适配（可选）**：在核心链路稳定后，补充移动端布局。

---

## 二、M5 任务清单（按优先级排序）

### P0 — 必须通过（阻塞项）

| 编号 | 任务 | 具体内容 | 交付物 | 协作方 |
|---|---|---|---|---|
| M5-W4-01 | history 页 Schema 驱动化 | 与 W3 确认 `src/backend/ui_schema.json` 的 `history.widgets` 中补充 `pie-chart` 和 `confidence-line-chart` 配置；W4 移除 `HistoryView.vue` 中的硬编码图表组件，改由 `SchemaRender` 渲染 | `HistoryView.vue` 更新、`src/backend/ui_schema.json` 变更申请（如需） | W3 |
| M5-W4-02 | detect 端到端联调 | 使用真实茶叶图片调用 `POST /api/detect`，确认前端能正确展示 `class_name`、`confidence`、`bbox`、`trace_id`；若后端返回字段或图片展示格式有变，立即上报 | 联调记录、截图 | W3 |
| M5-W4-03 | trace 端到端联调 | 用 detect 产生的真实 `trace_id` 调用 `GET /api/trace/{id}`，确认哈希链时间线可渲染，篡改检测可标红 | 联调记录、截图 | W3 |
| M5-W4-04 | 生产环境 baseURL 配置 | 将 `src/utils/request.js` 中的 `baseURL: ''` 改为读取环境变量 `import.meta.env.VITE_API_BASE_URL || ''`；新增 `.env.development`（空/默认）和 `.env.production`（按需配置） | `request.js`、`.env.*` | 无 |
| M5-W4-05 | 项目同步到 GitHub | 在 GitHub 创建/更新毕业设计仓库，将当前 workspace 完整推送（frontend/、src/、docs/、models/、tests/、requirements*.txt、scripts/、gen_*.py、变更文档等） | GitHub 仓库链接、提交记录 | PM/组长 |

### P1 — 强烈建议修复

| 编号 | 任务 | 具体内容 | 交付物 | 协作方 |
|---|---|---|---|---|
| M5-W4-06 | E2E 脚本可移植化 | 将 `@playwright/test` 或 `playwright-core` 加入 `frontend/package.json` 的 devDependencies；移除 `tests/e2e/manual_check.cjs` 中的硬编码绝对路径；提供 `npm run test:e2e` 脚本 | `package.json` 更新、`tests/e2e/manual_check.cjs` 重构 | 无 |
| M5-W4-07 | ECharts 按需引入优化 | 将 `import * as echarts from 'echarts'` 改为按需引入（line/pie/bar 等），减小构建 chunk 体积 | chart 组件更新 | 无 |
| M5-W4-08 | 训练曲线看板升级（可选） | 若 W2 提供训练日志 CSV/JSON 接口，用 ECharts 动态绘制 loss/mAP 曲线；否则保持当前 W2 PNG + test_metrics.json 方案 | `TrainingCurvePanel.vue` 更新 | W2 |

### P2 — 可选优化

| 编号 | 任务 | 具体内容 | 交付物 | 协作方 |
|---|---|---|---|---|
| M5-W4-09 | 移动端响应式适配 | 使用 Element Plus 的响应式布局（`<el-col :xs="24" :md="12" ...>`）优化手机/平板访问体验 | 页面布局更新 | 无 |
| M5-W4-10 | UI 走查与细节打磨 | 统一空状态、加载状态、错误提示；优化图表配色与主题一致性 | 视觉走查清单 | 无 |

---

## 三、协作边界与依赖关系

### W3（后端）
- **M5-W4-01 依赖 W3**：确认 `src/backend/ui_schema.json` 的 `history.widgets` 是否补充图表配置。若 W3 修改 schema，需同步更新 `docs/W3_M3_接口说明.md` 与 `W3_M3里程碑汇报_后端与溯源.md`。
- **M5-W4-02/03 依赖 W3**：确认 `POST /api/detect` 返回字段、图片保存路径、错误码；确认 `GET /api/trace/{id}` 在真实上链后的返回结构。

### W2（模型）
- **M5-W4-08 依赖 W2**：若需动态训练曲线，W2 需提供 `results.csv` 或等价 JSON 的访问方式（可通过 `/models/results.csv` 静态文件或新接口）。

### W5（测试集成）
- W5 在 M5 后期介入，使用 W4 重构后的 E2E 脚本跑全量巡检。
- W5 负责验证：`npm run build` 产物、`npm run test:e2e`、后端+前端同时启动后的注册/检测/溯源主链路。

---

## 四、验收标准

### M5 通过条件（必须全部满足）

- [ ] M5-W4-01 完成：history 页不再硬编码图表，改由 SchemaRender 根据后端 schema 渲染。
- [ ] M5-W4-02 完成：使用真实茶叶图片调用 `/api/detect`，前端正确展示等级、置信度、trace_id。
- [ ] M5-W4-03 完成：使用真实 trace_id 调用 `/api/trace/{id}`，哈希链时间线正确渲染。
- [ ] M5-W4-04 完成：生产环境 baseURL 可配置，`npm run build` 产物在非 dev proxy 环境下可指定后端地址。
- [ ] M5-W4-05 完成：项目已推送到 GitHub 仓库，PM/组长可访问。
- [ ] `npm run build` 成功，无编译错误。
- [ ] `npm run dev` 配合后端启动后，注册/登录/检测/历史/溯源/登出主链路可跑通。

### M5 加分项（非必须）

- [ ] M5-W4-06 完成：E2E 脚本可移植，W5 能在新环境直接运行。
- [ ] M5-W4-07 完成：构建产物 chunk 体积明显减小，无 500kB+ 警告。
- [ ] M5-W4-08 完成：训练曲线看板使用 ECharts 动态绘制。
- [ ] M5-W4-09 完成：移动端响应式适配。

---

## 五、工期与风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| W3 schema 调整引入接口变更 | 中 | W4 与 W3 对齐后，W3 同步更新接口文档与测试 |
| GitHub 推送涉及大文件（models/*.pt） | 中 | 使用 Git LFS 管理 `.pt` 文件，或在 `.gitignore` 中排除模型权重，仅保留下载说明 |
| W2 训练曲线 API 未就绪 | 低 | 保持静态 PNG 方案，动态化作为 P1/P2 |
| 移动端适配耗时超预期 | 低 | 列为 P2，核心链路稳定后再做 |

---

## 六、汇报要求

W4 完成 M5 后，按《汇报成果模板》提交：

1. **M5 里程碑汇报文档**：`W4_M5_里程碑汇报.md`
2. **端到端联调记录**：包含真实茶叶图片检测结果、trace_id、哈希链验证截图
3. **修复清单**：逐条说明 M4 审查问题的修复方式
4. **GitHub 仓库链接**与最近一次提交 hash
5. **构建产物验证**：`npm run build` 输出截图
6. **E2E 测试结果**：`npm run test:e2e` 运行结果或手动巡检截图

---

## 七、审批栏

| 角色 | 意见 | 签字/时间 |
|---|---|---|
| 项目经理（PM） | 同意按本推进函派发 W4 执行 M5 | 待填写 |
| 组长 | 请批复 | 待批复 |
| W4 接收确认 | 已接收，按推进函执行 | 待填写 |

---

## 八、附件

1. `W4_M4_里程碑汇报.md`
2. `W4_M4_代码审查结论.md`
3. `W3_M3_代码审查结论.md`
4. `W4_提示词.txt`
5. `W4_开工交接单.md`
