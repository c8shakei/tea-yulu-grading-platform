# W3 M3 里程碑完工审查结论

> 审查对象：W3 后端与溯源工程师提交的 M3 后端集成里程碑材料  
> 提交时间：2026-09-06  
> 当前提交组长审阅版本：v1

---

## 一、审查对象

W3 后端与溯源工程师提交的 M3 后端集成里程碑材料（共 6 份代码模块 + 4 份说明/脚本）：

| 序号 | 文件名 | 作用 |
|---|---|---|
| 1 | `src/backend/main.py` | FastAPI 应用入口与生命周期管理 |
| 2 | `src/backend/config.py` | 环境变量、路径与 JWT 配置 |
| 3 | `src/backend/database.py` | SQLite 连接、三表初始化与数据访问 |
| 4 | `src/backend/auth.py` | JWT 注册 / 登录 / 用户信息 / 登出 |
| 5 | `src/backend/detections.py` | 检测 / 历史记录 / 统计接口 |
| 6 | `src/backend/traceability.py` | 哈希链上链 / 溯源验证接口 |
| 7 | `src/backend/ui_schema.json` | 低代码页面配置 JSON |
| 8 | `src/hashchain/chain.py` | 哈希链核心：区块、链接、验证 |
| 9 | `docs/W3_M3_接口说明.md` | 接口清单、字段说明、数据库 ERD |
| 10 | `docs/W3_M3_哈希链验证说明.md` | 不可篡改验证流程说明 |
| 11 | `tests/test_backend.py` | 后端自测脚本 |
| 12 | `scripts/start_backend.py` | 一键启动后端服务脚本 |
| 13 | `requirements-backend.txt` | 后端最小依赖清单 |

---

## 二、实际交付物核验

已核对本地 `src/backend/`、`src/hashchain/`、`tests/`、`scripts/`、`docs/` 目录，核心文件均存在且可运行：

| 文件 | 说明 | 状态 |
|---|---|---|
| `src/backend/main.py` | FastAPI 入口， lifespan 初始化 DB / 模型 / 哈希链 | ✅ |
| `src/backend/config.py` | PROJECT_ROOT / MODEL_PATH / DB_PATH / UPLOAD_DIR / JWT secret | ✅ |
| `src/backend/database.py` | users / detections / hashchain 三表 + DAO | ✅ |
| `src/backend/auth.py` | 注册、登录、me、logout，bcrypt 哈希 | ✅ |
| `src/backend/detections.py` | `/api/detect`、`/api/detections`、统计接口 | ✅ |
| `src/backend/traceability.py` | `/api/trace`、`/api/trace/{id}` | ✅ |
| `src/backend/ui_schema.json` | appName / nav / pages 低代码配置 | ✅ |
| `src/hashchain/chain.py` | Block、HashChain、verify_block_dicts | ✅ |
| `docs/W3_M3_接口说明.md` | 接口说明文档 | ✅ |
| `docs/W3_M3_哈希链验证说明.md` | 哈希链验证说明文档 | ✅ |
| `tests/test_backend.py` | 覆盖全部 11 个接口的自测脚本 | ✅ |
| `scripts/start_backend.py` | 带 PYTHONPATH 的启动脚本 | ✅ |
| `requirements-backend.txt` | fastapi / uvicorn / python-jose / passlib 等 | ✅ |

---

## 三、关键指标

### 3.1 契约③ 接口实现情况

| 方法 | 路径 | 实现状态 |
|---|---|---|
| POST | `/api/auth/register` | ✅ 密码 bcrypt 哈希 |
| POST | `/api/auth/login` | ✅ OAuth2 form + JWT |
| GET | `/api/auth/me` | ✅ 返回当前用户信息 |
| POST | `/api/auth/logout` | ✅ 客户端配合清除 token |
| POST | `/api/detect` | ✅ 支持匿名与登录态，调用 W2 模型 |
| POST | `/api/trace` | ✅ 追加复核上链 |
| GET | `/api/trace/{id}` | ✅ 返回 valid + 整条 chain |
| GET | `/api/detections` | ✅ 按当前用户过滤 |
| GET | `/api/ui/schema` | ✅ 读取 ui_schema.json |
| GET | `/api/stats/overview` | ✅ 总检测 / 今日 / 等级分布 / 7 天趋势 |
| GET | `/api/stats/user/{user_id}` | ✅ 用户维统计 + 置信度趋势 |

### 3.2 统一响应格式

所有接口返回统一格式，已验证：

```json
{
  "code": 0,
  "message": "ok",
  "data": { ... }
}
```

### 3.3 自测结果

运行 `tests/test_backend.py`，全部 14 项检查通过：

```text
[OK] /health
[OK] /api/auth/register
[OK] /api/auth/login
[OK] /api/auth/me
[OK] /api/auth/logout
[OK] /api/detect anonymous
[OK] /api/detect authenticated
[OK] /api/trace
[OK] /api/trace/{id} verify
[OK] hashchain tamper detection
[OK] /api/detections
[OK] /api/ui/schema
[OK] /api/stats/overview
[OK] /api/stats/user/{user_id}
=== All tests passed ===
```

### 3.4 哈希链验证

- 每条检测独立成链，复合主键 `(trace_id, index)`。
- 创世区块 `prev_hash = "0" * 64`。
- 篡改任一字段后 `verify_block_dicts` 返回 `False`，自测脚本已验证。

---

## 四、变更控制状态

| 变更编号 | 内容 | 状态 |
|---|---|---|
| 交接单契约③ | 11 个 RESTful 接口与统一响应格式 | 已执行 |
| 交接单契约④ | 哈希链不可篡改验证 | 已执行 |
| CR-W1-004 | 低代码 UI Schema：`src/backend/ui_schema.json` + `GET /api/ui/schema` | 已执行 |
| CR-W1-005 | 只读统计接口，不新增业务表 | 已执行 |
| 代码审查修复项 1 | 补齐 tea-backend venv ML 依赖，自测可运行 | 已执行 |
| 代码审查修复项 2 | 测试脚本改为临时数据库，避免删除生产数据 | 已执行 |
| 代码审查修复项 3 | `POST /api/trace` 增加认证与所有者校验 | 已执行 |
| 代码审查修复项 4 | `/api/detect` 上传图片大小限制（默认 10 MB） | 已执行 |

- 数据库 schema 由交接单建议的 `hashchain(index INTEGER PRIMARY KEY AUTOINCREMENT)` 调整为复合主键 `(trace_id, index)`，以保证每条检测拥有独立链。该变更未改变接口契约，已在 `docs/W3_M3_哈希链验证说明.md` 中说明。
- `src/backend/db.py`、`src/backend/mock_detector.py`、`src/backend/routers/` 等旧文件已彻底清除，目录符合交接单要求。

---

## 五、阻塞风险

| 风险 | 现状 | 影响 |
|---|---|---|
| ~~测试脚本在当前环境无法运行~~ | **已修复**：tea-backend venv 已补齐 numpy/pillow/opencv-python/torch/torchvision/ultralytics，PowerShell 下自测通过 | 无 |
| 自测使用伪造 PNG | OpenCV 报 `IHDR chunk shall be first`，detector 回退为“等外” | 仅为测试数据问题，真实图片路径正常，不影响接口契约 |
| JWT secret 为本地默认值 | `TEA_JWT_SECRET` 未替换 | 生产部署前必须修改，否则存在安全风险 |
| 检测 mAP50-95 未达 0.90 | 当前主模型 0.702，目标差距 0.157 | 后端无法单独解决，需 W2/W4 后续通过数据增强、后处理、阈值调优补偿 |

当前无阻塞 W4 开工的硬风险。

---

## 六、待组长决策项

1. **是否确认 W3 M3 里程碑完成，批准 W4 前端立即开工？**
   - 全部 11 个接口已实现并自测通过；代码审查发现的 4 项阻塞/强烈建议问题已修复，建议批准。
2. **是否同意 `hashchain` 采用复合主键 `(trace_id, index)` 而非交接单原建议的单表自增主键？**
   - 理由：保证每条检测拥有独立溯源链，避免全局链混用导致验证复杂。
3. **生产部署前是否要求 W3 追加 `.env` 模板与 Docker 启动？**
   - 建议：当前先不阻塞 W4，Docker / 环境变量模板作为 M3 后续补丁或 M4 部署任务。
4. **是否要求 W3 补充真实图片端到端测试？**
   - 建议：由 W5 测试智能体在集成测试阶段补充，W3 当前自测已覆盖接口流程。

---

## 七、下一步建议

- **批准 W3 M3 里程碑完成**，W4 前端可立即对接 `/api/detect`、`/api/auth/login`、`/api/stats/overview`、`/api/ui/schema`。
- **W4 前端重点页面**：
  - 登录 / 注册页
  - 检测工作台（上传图片、展示等级/置信度/Bbox/trace_id）
  - 历史记录页
  - 溯源验证页（展示 chain 与 valid 状态）
  - 统计仪表盘（ECharts 饼图、折线图）
  - 低代码演示页（按 `ui_schema.json` 渲染）
- **W3 后续补丁**：生产环境替换 `TEA_JWT_SECRET`、补充 `.env.example`、按需补充 Docker。
- **W5 集成测试**：使用真实茶叶图片运行 `tests/test_backend.py` 的扩展用例，验证检测等级与 Bbox 输出。

---

## 八、备注

- 后端环境使用独立 venv：`C:/Users/wzd/.workbuddy/binaries/python/envs/tea-backend`，已补齐 numpy/pillow/opencv-python/torch/torchvision/ultralytics。
- 启动命令（PowerShell）：
  ```powershell
  cd C:/Users/wzd/Desktop/毕业设计
  C:/Users/wzd/.workbuddy/binaries/python/envs/tea-backend/Scripts/python.exe scripts/start_backend.py
  ```
- 启动后 Swagger UI：`http://127.0.0.1:8000/docs`，ReDoc：`http://127.0.0.1:8000/redoc`。
- 自测脚本使用临时数据库，不会删除生产 `data/tea_yulu.db`：
  ```powershell
  C:/Users/wzd/.workbuddy/binaries/python/envs/tea-backend/Scripts/python.exe tests/test_backend.py
  ```
- 所有交付物路径详见下方清单。

---

## 九、交付物地址清单

### 9.1 后端源代码

| 文件 | 绝对路径 |
|---|---|
| FastAPI 入口 | `C:\Users\wzd\Desktop\毕业设计\src\backend\main.py` |
| 配置模块 | `C:\Users\wzd\Desktop\毕业设计\src\backend\config.py` |
| 数据库层 | `C:\Users\wzd\Desktop\毕业设计\src\backend\database.py` |
| 认证模块 | `C:\Users\wzd\Desktop\毕业设计\src\backend\auth.py` |
| 检测/历史/统计接口 | `C:\Users\wzd\Desktop\毕业设计\src\backend\detections.py` |
| 哈希链接口 | `C:\Users\wzd\Desktop\毕业设计\src\backend\traceability.py` |
| 低代码 UI Schema | `C:\Users\wzd\Desktop\毕业设计\src\backend\ui_schema.json` |
| 哈希链核心 | `C:\Users\wzd\Desktop\毕业设计\src\hashchain\chain.py` |

### 9.2 说明文档

| 文件 | 绝对路径 |
|---|---|
| 接口说明文档 | `C:\Users\wzd\Desktop\毕业设计\docs\W3_M3_接口说明.md` |
| 哈希链验证说明 | `C:\Users\wzd\Desktop\毕业设计\docs\W3_M3_哈希链验证说明.md` |
| **本汇报文档** | `C:\Users\wzd\Desktop\毕业设计\W3_M3里程碑汇报_后端与溯源.md` |

### 9.3 测试与启动脚本

| 文件 | 绝对路径 |
|---|---|
| 自测脚本 | `C:\Users\wzd\Desktop\毕业设计\tests\test_backend.py` |
| 启动脚本 | `C:\Users\wzd\Desktop\毕业设计\scripts\start_backend.py` |
| 后端依赖 | `C:\Users\wzd\Desktop\毕业设计\requirements-backend.txt` |
