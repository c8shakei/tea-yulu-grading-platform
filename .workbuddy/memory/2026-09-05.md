# 2026-09-05 工作日志

## W2 变更申请 CR-W1-002 审查与批复（09:10）
- 用户转发 `变更申请_CR-W1-002_训练脚本yolov8s多规模支持.docx`。
- 文档内容：
  - 变更类型：环境与部署变更（模型规模可配置化）。
  - 变更内容：train_detect.py / train_cls.py 通过 `YOLO_MODEL_SIZE` 环境变量切换 yolov8n/yolov8s；产物文件名追加规模后缀物理隔离；yolov8s 自动降低 batch size 防 OOM；sync_checkpoints.py 同步支持规模切换。
  - 变更原因：yolov8n 实测 mAP50-95=0.702（目标≥0.90），特级类 mAP50-95 仅 0.534，需用 yolov8s（11.1M 参数）做骨干规模消融。
  - 待决策项：论文主模型是否升为 yolov8s（影响接口契约②权重路径与 W3 加载逻辑）。
- 组长批复：
  - 批准方案 A（环境变量可配 + 文件名后缀隔离）。
  - 维持 yolov8n 为当前主模型与部署默认；yolov8s 仅作为消融对照。
  - 主模型切换的二次裁定口径：yolov8s 相比 yolov8n mAP50-95 提升 ≥0.05 且本机 CPU 单张推理 <200ms，方可切换；否则保留 yolov8n 主模型。
  - 备注：变更申请人字段填写为 W1，但变更对象属 W2 工作域，结合项目经理确认，实际由 W2 发起并实施，W1 负责脚本工程化与回归校验。
- 产出：`变更批复书_CR-W1-002.docx`（29段、1表），生成器 `gen_change_approval_cr002.py`。
- 后续：W2 可在 Colab 立即跑 yolov8s 实验；W3 仍按 best_detect.pt（yolov8n）对接。

## W2 变更申请 CR-W1-003 审查与批复（11:03）
- 用户转发 `变更申请_CR-W1-003_训练平台迁移Kaggle与脚本可移植化.docx`。
- 背景：Colab 免费 GPU 额度已用完，yolov8s 追加实验无法继续，需换平台。
- 变更内容：
  - train_detect.py / train_cls.py / sync_checkpoints.py 新增 `WORK_DIR` / `SYNC_TARGET` / `DATA_ROOT` 环境变量，支持 Colab + Google Drive 与 Kaggle 双平台。
  - 数据集定位自适应：优先 `WORK_DIR/datasets/tea_yulu`，其次 Colab 本地，再回退 Drive zip。
  - `resolve_data_yaml()` 改为写到可写目录，规避 Kaggle `/kaggle/input` 只读问题。
  - yolov8s 实验执行环境由 Colab T4 迁移至 Kaggle 免费 P100。
- 变更原因：Colab 免费 GPU 滚动限额不可控，继续等待阻塞 M2；Kaggle 免费 P100 额度约 30h/周，零成本且性能更优。
- 组长批复：
  - 批准方案 A（脚本平台可移植改造 + 迁移 Kaggle）。
  - 接口契约②与产物命名完全不变；W3/W4 无需调整。
  - 默认环境变量仍兼容 Colab 旧流程；Kaggle 下显式设置 `WORK_DIR=/kaggle/working`、`SYNC_TARGET=/kaggle/working/BISHE/models`、`YOLO_MODEL_SIZE=yolov8s`。
  - 备注：变更申请人字段同样填写为 W1，实际属 W2 工作域，以文件编号 CR-W1-003 为准。
- 产出：`变更批复书_CR-W1-003.docx`（31段、1表），生成器 `gen_change_approval_cr003.py`。
- 后续：W2 需在 Kaggle 上传数据集 zip 与三脚本，开启 P100 GPU，按批复环境变量执行 yolov8s 训练。

## CR-W1-004 范围扩展：用户认证 + 多页面 + 低代码配置层（15:18）
- 用户提出：前端页面太简单，希望增加登录/注册，做成完整多页面网站，并融合低代码技术。
- 可行性评估：完全可行。W1/W2 不受影响；W3/W4/W5 增量扩展。
- 低代码方案选择：**C 方案（内生式 JSON Schema 配置层）**，原因：
- 零外部商业平台依赖，符合“全部开源免费”约束；
- 不引入 Appsmith/Budibase 等额外部署成本；

## W2 M2 里程碑完工审查（19:53）
- 用户通知 W2 已完成 M2 模型训练，提交 6 份材料待审：
  - `W2_M2_过程性资料追溯档案.docx`
  - `W2_M2里程碑汇报_版本说明.md`
  - `W2_M2里程碑汇报_模型训练.docx`（原始归档）
  - `W2_M2里程碑汇报_模型训练_v1_yolov8n基线.docx`
  - `W2_M2里程碑汇报_模型训练_v2_yolov8s消融完成.docx`（当前提交组长审阅版）
  - `W2_M2里程碑汇报_模型训练_提交组长版.docx`
- 通过 python-docx 提取并核对全部文本与表格；确认本地 `models/` 已存在 4 个权重与指标文件。
- 关键指标：
  - yolov8n 检测：mAP50-95=0.702 / mAP50=0.880 / P=0.799 / R=0.867
  - yolov8s 检测：mAP50-95=0.743 / mAP50=0.906 / P=0.884 / R=0.851（+0.041）
  - yolov8n-cls 分类：Top-1=0.904 / Top-5=1.000
  - yolov8s-cls 分类：Top-1=0.923 / Top-5=1.000（+0.019）
- 关键结论：
  - CR-W1-002/003 已闭合；yolov8n 基线未被覆盖，yolov8s 产物带后缀隔离。
  - 检测 mAP50-95 仍未达 0.90 目标（0.743，差距 0.157）；特级类 mAP50-95 仅 0.558。
  - 接口契约②保持不变，W3 仍按 `best_detect.pt`（yolov8n）对接。
- 待决策项（需组长批复）：
  1. 是否确认 M2 里程碑完成，批准进入 W3/W4 阶段。
  2. 是否将 yolov8s 升为主模型（当前未满足二次裁定口径：提升需 ≥0.05 且 CPU 单张 <200ms，提升仅 0.041）。
  3. 是否继续追加特级类优化实验，或在 W3/W4 通过数据增强/后处理补偿。
- 建议：维持 yolov8n 主模型，批准 W2 M2 完成，将特级指标优化与 yolov8s 主模型切换决策延至 W3/W4 迭代中根据实测再定；W3 立即开工后端集成。
  - 答辩亮点：改 ui_schema.json 即可实时改变导航与页面。
- 组长批复：同意方案 C，采用 JWT 认证 + Vue Router 多页面 + SchemaRender 引擎。
- 关键决策：
  - 认证：JWT 无状态 token，前端存 localStorage，请求加 Authorization: Bearer。
  - 检测接口允许匿名（便于现场演示），历史记录 /api/detections 必须登录后按用户过滤。
  - 低代码层第一阶段用于导航生成和部分页面渲染；/detect 专用页不完全由 schema 驱动。
- 同步更新：
  - 接口契约③：增加 /api/auth/*、/api/detections、/api/ui/schema。
  - 派工指导手册：重新生成，含更新后的契约与提示词。
  - W3/W4/W5_提示词.txt：已固化 CR-W1-004 约束。
- 产出：
  - `变更申请_CR-W1-004_增加用户认证多页面与低代码配置层.docx`（34段、2表），生成器 `gen_change_approval_cr004.py`。
  - `变更批复书_CR-W1-004.docx`（38段、1表）。
  - `派工指导手册_农产品分级溯源平台.docx`（68段、2表）。
  - `W1_提示词.txt` ~ `W5_提示词.txt`。
- 工期影响：预计 +2 天，仍在 30 天硬基线内。
- 后续：W3 在 M3 实现认证/历史/UI Schema；W4 在 M4 实现多页面 + SchemaRender；W5 补充端到端测试。

## CR-W1-005 范围扩展：数据可视化 + 统计看板 + 哈希链可视化（15:49）
- 用户提出：页面交互点仍可扩充，希望加入 ECharts 图表；同时征求更多“低风险、高答辩价值”的补充想法。
- 可行性评估：完全可行。W1/W2 不受影响；W3/W4/W5 增量扩展。
- 可视化方案选择：**内生式 ECharts 可视化层（C 方案）**，原因：
  - 完全开源免费（echarts/vue-echarts），不引入外部商业平台；
  - 不替换现有架构，只在现有检测/溯源/认证数据上做只读聚合与展示；
  - 答辩演示点显著增加：首页仪表盘、历史图表、哈希链时间线、训练曲线看板。
- 组长批复：同意方案 A，打包批准以下内容：
  - 首页 /：统计卡片 + 等级分布饼图 + 近 7 天检测趋势折线图。
  - 历史记录页 /history：检测表格 + 等级分布饼图 + 平均置信度趋势折线图。
  - 溯源验证页 /trace：哈希链时间线，篡改区块标红。
  - 低代码演示页 /admin/schema：训练曲线看板（读取 W2 的 metrics JSON）。
- 关键决策：
  - 最终页面数：**8 个页面**（/login、/register、/、/detect、/history、/trace、/profile、/admin/schema）。
  - 新增接口：GET /api/stats/overview、GET /api/stats/user/{user_id}，均为只读聚合，不新增业务表。
  - 图表数据禁止硬编码，必须来自后端接口或 W2 metrics JSON。
  - 训练曲线看板不修改 W2 代码，直接消费已有 metrics JSON。
- 同步更新：
  - 接口契约③：增加 /api/stats/overview、/api/stats/user/{user_id}。
  - 派工指导手册：重新生成。
  - W3/W4/W5_提示词.txt：已固化 CR-W1-005 约束。
- 产出：
  - `变更申请_CR-W1-005_增加数据可视化统计看板与哈希链可视化.docx`（31段、3表），生成器 `gen_change_approval_cr005.py`。
  - `变更批复书_CR-W1-005.docx`（28段、1表）。
  - `派工指导手册_农产品分级溯源平台.docx`（68段、2表）。
  - `W1_提示词.txt` ~ `W5_提示词.txt`（W3/W4/W5 已更新）。
- 工期影响：预计额外 +1.5 天；累计 CR-W1-004 + CR-W1-005 约 +3.5 天，仍在 30 天硬基线内。
- 后续：W3 实现 stats 接口；W4 引入 ECharts 并完成 6+ 图表组件；W5 补充统计与图表渲染测试。
- 最终可演示场景：注册 → 登录 → 首页仪表盘 → 上传检测 → 生成溯源 → 历史图表 → 哈希链时间线 → 训练曲线看板 → 低代码实时改页面。

## W3 开工派发（21:17）
- 用户批准审查建议，决定立即启动 W3 后端与溯源开发。
- 已更新 `W3_提示词.txt`：明确 W2 已交付、主模型仍为 `models/best_detect.pt`（yolov8n）、yolov8s 仅作论文消融。
- 已编制 `W3_开工交接单.md`：包含 W2 产物清单、接口契约、数据库 Schema、哈希链契约、目录结构、交付物清单、质量要求、环境检查命令。
- W3 任务：FastAPI + SQLite + JWT 认证 + 检测接口 + 哈希链溯源 + 统计接口 + UI Schema 配置。
- M3 目标：后端可用（D16 左右），W4 可对接 `/api/detect`、`/api/auth/login`、`/api/stats/overview`、`/api/ui/schema`。
- 阻塞上报口径：W2 推理调用异常、接口字段与 W4 冲突、统计需求无法由现有 detections 表支撑时立即上报。

## W3 M3 实现审查（09-06 16:13）
- 用户要求审查 W3 提交的 13 个文件：后端源码（main/config/database/auth/detections/traceability/ui_schema.json）、hashchain/chain.py、文档、测试、启动脚本、依赖清单。
- 已逐项阅读代码并实测 `scripts/start_backend.py`，确认后端可正常启动（`Uvicorn running on http://0.0.0.0:8000`）。
- 审查结论：
  - 11 个接口全部实现，统一响应格式、JWT 认证、SQLite 三表、哈希链不可篡改验证、UI Schema、统计接口均符合契约③④与 CR-W1-004/005。
  - `TeaGradeDetector` 在 lifespan 中一次性加载，符合性能要求。
  - 发现阻塞项：当前环境 pytest 导入 torch 触发 Windows access violation，测试脚本 `tests/test_backend.py` 无法跑通。
  - 发现必须修复项：测试脚本默认删除生产数据库 `data/tea_yulu.db`。
  - 发现建议项：`POST /api/trace` 未认证、上传图片未限制大小/清理策略。
  - `docs/W3_M3_接口说明.md` 读取被安全审批拦截，未能在本次审查。
- 产出：`W3_M3_代码审查结论.md`，给出有条件通过结论与修复清单。
- 下一步：W3 修复测试环境问题（推荐在 tea-backend venv 内安装 ML 依赖）与测试脚本破坏性删除；W5 随后跑通全量接口测试；W4 可提前基于已启动后端进行联调。

## W3 M3 修复复核（09-06 16:51）
- 用户提交 W3 修复结果，要求复核。
- W3 修复内容：tea-backend venv 补齐 numpy/pillow/opencv-python/torch/torchvision/ultralytics；测试脚本改用临时数据库；`POST /api/trace` 增加认证与所有者校验；`/api/detect` 增加 10MB 上传限制；`start_backend.py` 移除 PYTHONPATH hack；`requirements-backend.txt` 增加 ML 依赖；文档已更新。
- 独立验证：
  - 依赖检查：venv 内已包含 numpy 2.1.3 / opencv-python 5.0.0.93 / pillow 12.3.0 / torch 2.14.0 / torchvision 0.29.0 / ultralytics 8.4.142。
  - PowerShell 下运行 `tests/test_backend.py`：全部 14 项检查通过。
  - Bash 下启动 `scripts/start_backend.py`：`/health` 返回 `{"code":0,"message":"ok","data":{"status":"up"}}`。
- 复核结论：4 项阻塞/强烈建议问题已全部修复，测试可运行、服务可启动。
- 残余轻微项：测试用伪造 PNG 时 OpenCV 报 `IHDR chunk shall be first` 并回退为“等外”，属测试数据问题；`docs/W3_M3_接口说明.md` 本助手仍无法读取，但 W3 已声明更新。
- 产出：`W3_M3_代码审查结论.md` 已追加“修复复核记录”章节。
- 建议：组长批复 M3 后端里程碑通过，W4 前端立即开工。
