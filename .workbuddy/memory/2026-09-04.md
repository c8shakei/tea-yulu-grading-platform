# 2026-09-04 工作日志

## 重生成 4 份 docx（解除上一轮 shell 故障阻塞）
- 用户开放权限后，重试此前被 Bash/PowerShell 故障卡住的重生成任务，成功产出：
  - `项目章程与进度计划_农产品分级溯源平台.docx`（08:54）
  - `软件需求规格说明书_SRS.docx`（08:54）
  - `AI指挥手册_农产品分级溯源平台.docx`（08:55）
  - `开题报告_农产品分级溯源平台.docx`（08:55）
- 运行 `gen_handbook.py` 时遇到 SyntaxError：第 145 行字符串内误用 ASCII 双引号 `"` 提前闭合字符串，已改为中文引号 `""` 修复并复跑成功。
- 已彻底删除作废的 `patch3.py`（其改动此前已手动并入各生成器，绝不可再运行）。

## 多对话派工体系（组长统筹 + 5 员工并行）
- 用户要求以「项目组长」身份把毕设派发到多个对话，避免上下文过长、提升可追溯性。产出：
  - `派工指导手册_农产品分级溯源平台.docx`（44KB，68段2表，6章齐全）
  - `W1_提示词.txt` ~ `W5_提示词.txt`（可直接复制进新对话）
  - 生成器：`gen_delegate_handbook.py`
- **员工编制（已定）**：W1 数据工程 / W2 视觉模型 / W3 后端与溯源 / W4 前端 / W5 测试与集成；组长保留章程·SRS·进度·接口契约·集成决策·论文装配。论文不单独占名额（可按需拆第6个）。
- **依赖链**：W1→W2→W3→W4→W5；里程碑映射 M1(D3-4)/M2(D10)/M3(D16)/M4(D24)/M5(D30)。
- **四份协同接口契约（关键，避免返工）**：
  - ① YOLO 数据集契约（datasets/tea_yulu/{split}/{images,labels}，类别固定 0特级/1一级/2二级/3等外）
  - ② 模型推理 `detect(image)->{class_id,class_name,confidence,bbox}`，best.pt
  - ③ FastAPI 契约 `/api/detect`、`/api/trace`、`/api/trace/{id}`，统一包裹 `{code,message,data}`
  - ④ 哈希链 `{index,timestamp,prev_hash,data_hash,hash}`，创世 prev_hash="0"*64
- 汇报模板字段：编号/日期/里程碑/完成项/交付物路径/关键指标/契约变更/阻塞风险/下一步/需组长决策项。

## 工具路径踩坑（重要）
- 新建 docx 按 `tencent-docs-routing` 应走 `tencent-docx` 插件 full_pipeline（S1 doc-writer→S2 doc-formatter→S3 doc-converter→html-to-docx）。
- **但本机 `$HOME/.venv-html-to-docx` 未安装**，其 setup 首跑需联网拉 uv / python-build-standalone / PyPI；本机有网络受限前科（torch CUDA 12B/s），此路径不可靠。
- 按路由 Skill 的降级条款（直接路径失败可用 Bash/python，须先告知用户），改用已验证的 python-docx 生成器路径（与项目其余 docx 同源），已如实向用户说明。
- 注意：doc-writer/doc-formatter 在 `agents/` 目录而非 `skills/`，易找错。

## 当前状态
- 全部 4 份核心文档已是最新版（含写死内容：答辩 2027-05 中旬、数据集 TeaLeafAgeQuality、Colab GPU 训练、Vue+FastAPI、恩施玉露茶）。
- Shell 工具恢复正常。
- 下一步建议：初始化 Git 仓库 → 启动 W1 数据工程对话（最先、无依赖）→ 同步启动 W2 等其数据就绪。

## W1 数据工程启动（10:00–）
- 确认 TeaLeafAgeQuality 数据集：Mendeley Data DOI 10.17632/7t964jmmy3，CC BY 4.0，全量 zip 4.59 GB，含 raw/annotated/augmented 三版。
- 编写并行分片下载器 `scripts/w1_download_dataset.py`（8 并发、断点续传、SHA256 校验）。首次运行因 HEAD 请求 S3 预签名 URL 403 失败，改为 GET Range 请求修复。
- 下载过程中曾停滞在 20/88 分片（1.17 GB），已重启续传；当前 27.8%。
- 编写 `scripts/w1_extract_prepare.py`（自动识别 annotated 子集、T1–T4→特级/一级/二级/等外 映射）。
- 编写 `scripts/w1_split_dataset.py`（7:2:1 分层划分、固定种子 42、生成 data.yaml）。
- 编写 `scripts/w1_augment.py`（albumentations 仅增强 train，同步 bbox）。
- 编写 `数据说明.md`（含来源、映射、预处理、划分、已知问题）。
- 创建 `.gitignore`，排除 datasets/、*.zip、*.pt、runs/ 等大文件。
- **阻塞风险/需组长决策**：C 盘仅剩 6 GB，不足以容纳 4.59 GB zip + 解压图像 + 增强输出。方案：数据集实际存 D 盘（`D:\BISHE_DATA\datasets\tea_yulu`），项目根目录用 Windows 目录 junction 映射到 `datasets/tea_yulu`，保持接口契约路径不变。
- 已生成正式变更申请报告：`变更申请_CR-W1-001_数据集存储位置.docx`，含变更内容、原因、影响分析、三套可选方案及组长审批栏，待用户批复。

## W1 里程碑 M1 完成（14:50）
- 组长批复 `变更批复书_CR-W1-001.docx`：同意 D 盘 + junction 方案。
- zip 88/88 分片下载完成并合并（4.59 GB，校验可打开，18104 条目）。
- **数据集实测结构**（与原假设不同，已适配）：
  - `TeaLeafAgeQuality(Annotated)/{train,valid,test}/{images,labels}` = 2208 对（选用）
  - `Annotated and Augmented` = 5298 对（弃用，W1 自行增强）
  - `Raw Data/Category A–D` = 2208 图无标注（弃用）
  - 标注 class_id 0–3 实测对应 A/B/C/D（各类 551/616/508/520 ≈ Raw 562/615/508/523），即 0特级/1一级/2二级/3等外。
- 提取脚本两处修复：① images/labels 是兄弟目录需按 split 根聚合；② 保留 Roboflow 哈希后缀防跨子集同名覆盖。13 张空标注背景图跳过（0.59%），实入册 2195 对。
- 划分（种子 42）：train 1537 / val 439 / test 219，分层均衡。
- junction 已由 PowerShell `New-Item -ItemType Junction` 建好：`datasets/tea_yulu → D:\BISHE_DATA\datasets\tea_yulu`（cmd mklink 在 Git Bash 下调用失败，用 PS 可靠）。
- 项目根 `data.yaml`（path: datasets/tea_yulu）+ 数据集内便携副本（path: .）均已生成。
- 增强完成：train 1537 → 4611（AUG_FACTOR=2），val/test 不增强；全量校验 0 异常标注行。
- `数据说明.md` 已回填全部真实数值（规模表、类别分布表、13 张空标注说明、A–D 映射确认）。
- **最终规模**：train 4611 / val 439 / test 219，合计 5269；bbox 分布四舍五入均衡（最大/最小比 1.21）。
- 待办：向组长汇报 M1 完成 → W2 可启动（Colab 取数方案见数据说明第 7 节）。

## M1 终检与汇报（15:05）
- 终检脚本 `scripts/w1_final_check.py` 首轮 21/22 PASS：**split_manifest.csv 只有 12 行汇总而非逐样本清单**，已修复：
  - `w1_split_dataset.py` 的 write_manifest 改为输出逐样本清单（split_manifest.csv）+ 汇总（split_summary.csv）；
  - 一次性修复脚本 `scripts/w1_fix_manifest.py` 从现有目录重建（排除 _aug 文件，类别取标注首行），2195 行，汇总与原值一致。
- 修复后终检 **22/22 全部 PASS**。
- 按汇报模板生成正式汇报：`W1_M1里程碑汇报_数据就绪.docx`（编号 W1-M1-REPORT-001），含完成项/交付物路径表/关键指标/契约变更（无）/风险/下一步/2 项待组长决策。
- 踩坑：bash 内嵌 python -c 含转义引号会语法错误，复杂校验一律写脚本文件再跑；cmd mklink 在 Git Bash 下不可靠，junction 用 PowerShell New-Item。

## 组长审查 W1 M1 汇报并批准启动 W2（15:10）
- 读取 `W1_M1里程碑汇报_数据就绪.docx` 与 `数据说明.md`，并复跑 `scripts/w1_final_check.py`。
- 终检结果：**22/22 PASS**；CR-W1-001 三项强制约束全部满足（junction 可达、data.yaml 无盘符绝对路径、Colab 取数方案已写明）。
- 决策批复：
  1. **Colab 取数**：批准方案 A（Google Drive 上传挂载）为主方案，方案 B（Mendeley 重下）为 fallback。
  2. **检测 vs 分类**：主模型维持 **YOLOv8 检测**（按接口契约②输出 bbox），同时要求 W2 训练一个 **yolov8n-cls 分类对照模型**，用于论文对比实验；W3/W4 仍以检测接口为准。
- 非阻塞改进项：建议 W1 在待命期间把 13 张空标注背景图作为负样本补入 train（空 label 文件），提升检测模型鲁棒性；W2 可并行启动。
- 结论：**M1 通过，W2 立即启动。**
- 生成正式存档文档：`M1验收批复书_W2启动令.docx`（32段、4表），含基本信息、验收结论、验收口径核对表、组长决策批复、非阻塞改进项、W2 启动令、生效与签署。生成器：`gen_m1_approval_w2.py`。

## A 组智能体团队组建与 W2/W3 并行启动（15:27）
- 用户启用智能体团队管理，选择方案 A：组建 W2 视觉模型 + W3 后端溯源并行小分队。
- 创建任务跟踪：Task #16（W2 M2 模型训练）、Task #17（W3 M3 后端可用）。
- 派发智能体：
  - `w2-vision-engineer`：产出训练脚本、推理封装、文档；当前代码脚手架完成，真实训练待 Colab 导出权重。
  - `w3-backend-engineer`：产出 FastAPI + SQLite + 哈希链代码、文档、测试脚本；功能跑通。
- 组长复核发现两个问题并修复：
  1. `python-multipart` 未安装导致 W3 `scripts/test_m3.py` 失败 → `pip install python-multipart` 后 6/6 PASS。
  2. `src/backend/mock_detector.py` 返回像素坐标 bbox，与契约②归一化 [0,1] 不符 → 改为归一化坐标。
- 状态：W2/W3 代码结构均就绪，Task #16/#17 标记为 in_progress；阻塞点分别是 Colab 训练导出权重、W2 真实权重替换 mock。
- 生成过程文档：`团队组建与任务派发_W2W3_A组.docx`（32段、6表），记录组建决策、成员职责、任务派发、复核结果、问题修复、当前状态与下一步。生成器：`gen_team_formation_w2w3.py`。

## A 组后续操作步骤文档化（16:06）
- 用户要求给出具体操作步骤，生成 `W2W3_A组后续操作步骤.docx`（50段、2表），覆盖：
  1. 第一阶段：用户准备数据集（确认数据、打包上传 Google Drive、Colab 挂载）
  2. 第二阶段：W2 在 Colab 训练模型（解压、上传脚本、训练、导出 best.pt）
  3. 第三阶段：W3 替换 mock 并集成验证（改 main.py、启动后端、跑 test_m3.py）
  4. 第四阶段：W2/W3 同步提交里程碑汇报
  5. 第五阶段：组长验收 checklist 与启动 W4 条件
- 文档包含可直接复制的 PowerShell/Colab 命令块、验收 checklist、常见卡点处理。生成器：`gen_next_steps.py`。

## 数据集校验与打包脚本（16:17）
- PowerShell 脚本 `scripts/check_dataset.ps1` 因 UTF-8 无 BOM 被系统编码页解析乱码，已补充 Python 版 `scripts/check_dataset.py`，结果正确（train 4611 / val 439 / test 219，全部 PASS）。
- 新增 `scripts/zip_dataset.ps1` 用于一键把 `D:\BISHE_DATA\datasets\tea_yulu` 打包成 `D:\BISHE_DATA\tea_yulu_dataset.zip`（实测数据集大小约 772 MB，远小于 zip 原始 4.59 GB，Google Drive 15 GB 空间足够）。
- 用户要求给出具体操作步骤，生成 `W2W3_A组后续操作步骤.docx`（50段、2表），覆盖：
  1. 第一阶段：用户准备数据集（确认数据、打包上传 Google Drive、Colab 挂载）
  2. 第二阶段：W2 在 Colab 训练模型（解压、上传脚本、训练、导出 best.pt）
  3. 第三阶段：W3 替换 mock 并集成验证（改 main.py、启动后端、跑 test_m3.py）
  4. 第四阶段：W2/W3 同步提交里程碑汇报
  5. 第五阶段：组长验收 checklist 与启动 W4 条件
- 文档包含可直接复制的 PowerShell/Colab 命令块、验收 checklist、常见卡点处理。生成器：`gen_next_steps.py`。

## 数据集上传 Google Drive 成功（16:48）
- 用户确认已将 `tea_yulu_dataset.zip`（约 772 MB）上传到 Google Drive 根目录（https://drive.google.com/drive/my-drive）。
- 注意：脚本 `train_detect.py` / `train_cls.py` 默认从 `MyDrive/BISHE_DATA/datasets/tea_yulu` 读取解压后的数据集；由于 zip 在 Drive 根目录，W2 需在 Colab 中先把根目录 zip 解压到 `/content/datasets/tea_yulu`，再运行训练脚本。
- W2 已具备开工条件：数据在 Drive 根目录、训练脚本在本地项目 `src/models/`、便携式 `data.yaml`（path: .）随数据集一并上传。
- 下一步：把 W2 启动指令（挂载 Drive → 解压 → 运行训练 → 导出 best.pt）发给 W2。
