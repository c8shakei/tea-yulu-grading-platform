# CR-W1-004 变更处理概览

## 完成内容
1. 评估并批准用户提出的范围扩展请求：前端增加登录/注册、升级为多页面网站、融合低代码技术。
2. 选择 **C 方案（内生式 JSON Schema 配置层）**，拒绝外接商业/开源低代码平台（与开源免费约束冲突）。
3. 更新接口契约③：增加 `/api/auth/*`、`/api/detections`、`/api/ui/schema`。
4. 更新 W3/W4/W5 员工提示词，固化 CR-W1-004 约束。
5. 重新生成 `派工指导手册_农产品分级溯源平台.docx` 和 `W1_提示词.txt` ~ `W5_提示词.txt`。
6. 生成 `变更申请_CR-W1-004_...docx` 与 `变更批复书_CR-W1-004.docx`。
7. 记录到 `2026-09-05.md` 工作日志。

## 关键决策
- 认证方式：JWT（无状态 token）。
- 检测接口：允许匿名；历史记录必须登录后按用户过滤。
- 低代码层：后端维护 `ui_schema.json`，前端 SchemaRender 引擎动态渲染导航和部分页面；/admin/schema 提供 JSON 编辑器用于答辩演示。
- W1/W2 完全不受影响。

## 交付物
- `变更申请_CR-W1-004_增加用户认证多页面与低代码配置层.docx`
- `变更批复书_CR-W1-004.docx`
- `派工指导手册_农产品分级溯源平台.docx`
- `W3_提示词.txt`、`W4_提示词.txt`、`W5_提示词.txt`（已更新）
- `W1_提示词.txt`、`W2_提示词.txt`（保持不变，但随生成器重新生成）
- 生成器：`gen_change_approval_cr004.py`（保留）

## 后续事项
- W3 在 M3 实现认证、历史记录、UI Schema 接口。
- W4 在 M4 实现 Vue Router 多页面与 SchemaRender 引擎。
- W5 补充认证、历史、低代码演示的端到端测试。
