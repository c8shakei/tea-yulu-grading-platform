# W5 M6 组长批复

**批复人：** 组长  
**接收人：** PM / W5 / W3 / W4  
**日期：** 2026-09-07  
**阶段：** M6 — 答辩前全量测试与集成收尾  

---

## 一、批复结论

**W5 M6 里程碑：通过。**

W5 提交的测试与集成材料完整，验收指标全部达成，P0/P1 阻塞缺陷已清零。同意项目进入答辩前最终彩排阶段。

---

## 二、验收指标复核

| 验收条件 | 要求 | 实际 | 组长复核 |
|---|---|---|---|
| 后端接口测试 | ≥ 30 个用例，通过率 ≥ 95% | 32 个用例，通过率 100% | ✅ 通过 |
| 前端 E2E 可复现性 | 干净目录跑通 | 已通过（本地源 clone） | ✅ 通过 |
| 真实茶叶图片端到端 | 链路跑通 | 特级 / 73.61%，trace_id 与哈希链验证通过 | ✅ 通过 |
| 跨用户数据隔离 | 验证通过 | detections / stats / trace 均已隔离 | ✅ 通过 |
| 缺陷清单 P0/P1 | ≤ 3 个或已全部修复 | 0 个未修复 P0/P1 | ✅ 通过 |
| 答辩演示就绪 | 明确结论 | 可答辩演示 | ✅ 通过 |

---

## 三、关键问题处理

### BUG-01：GET /api/trace/{id} 数据隔离漏洞（P1）

**状态：已修复并验证。**

组长现场复核确认 `src/backend/traceability.py::get_trace` 原实现未做用户隔离，任何人均可读取任意 trace 的哈希链。已直接修复：

- 增加 `require_user` 依赖，未登录返回 401。
- 已认证用户检测的 trace，仅 owner 或 admin 可读取，否则返回 403。
- 匿名检测 trace（user_id 为 NULL）允许任何登录用户读取，保持向后兼容。
- 同步在 `tests/test_backend.py` 新增 `test_trace_isolation` 用例。

**验证结果：**
- `python tests/test_backend.py`：32 个用例全部通过。
- `npm run test:e2e`：端到端链路全量通过。

### BUG-07：UI Schema 页面数量口径（P3）

**状态：已确认。**

项目共 9 个页面：
- 7 个主业务页面由 UI Schema 驱动：`/`、`/detect`、`/history`、`/trace`、`/profile`、`/lowcode`、`/training`。
- 2 个认证前置页面采用硬编码路由：`/login`、`/register`。

文档中"9 个页面"包含 login/register；schema 中 7 个页面描述业务页面，两者不矛盾。统一按此口径对外解释。

---

## 四、残留建议项（非阻塞）

以下问题不影响 M6 通过，但建议在答辩前酌情处理：

| 编号 | 问题 | 归属 | 建议处理方式 |
|---|---|---|---|
| BUG-02 | 后端 HTTPException 返回 `{detail: ...}`，未统一为 `{code, message, data}` | W3 | 答辩前统一错误格式 |
| BUG-03 | 注册接口未校验弱密码 | W3 | 答辩前增加 ≥ 6 位校验 |
| BUG-04 | `/history` 手机尺寸表头换行/截断 | W4 | 答辩前优化小屏表格可读性 |
| BUG-05 | 个人中心总检测数显示为 0 | W4 | 答辩前修复统计刷新逻辑 |
| BUG-06 | GitHub 直接 clone 在当前环境失败 | W5 / 组长 | 答辩前确认网络，准备离线部署包 |

---

## 五、放行条件

M6 通过后，项目进入答辩前最终彩排。以下事项需在答辩前完成：

1. **离线部署包**：由 PM/组长准备，包含项目源码、模型权重、依赖安装脚本，确保答辩环境无 GitHub 网络也能部署。
2. **演示脚本彩排**：按 `W5_M6_答辩前测试结论.md` 中的答辩脚本完整走 2 遍以上。
3. **建议项可选修复**：BUG-02/03/04/05 中至少修复 BUG-05（个人中心统计），避免答辩时被评委追问。
4. **W2 训练曲线接口**：如时间允许，W2 提供 CSV/JSON 接口替换静态 PNG；时间不足则维持现状。

---

## 六、签名栏

| 角色 | 签名 | 日期 |
|---|---|---|
| 组长 | 已批复 | 2026-09-07 |
| PM | 待确认 | - |
| W5 | 已交付 | 2026-09-07 |

---

**备注：** 本批复同时抄送 W3/W4，BUG-01 由组长直接修复，W3 需在后续版本中复核并学习该修复模式。
