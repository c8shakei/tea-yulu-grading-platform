# W4 M4 前端里程碑审查结论

> 审查时间：2026-09-06  
> 审查人：项目经理助理  
> 审查范围：W4 提交的 `frontend/` 全部代码、`W4_M4_里程碑汇报.md`、E2E 自测脚本与截图

---

## 一、总体评价

W4 M4 前端**功能完整、可构建、可运行、已完成后端联调**。9 个页面、6 个 ECharts 组件、SchemaRender 引擎、Pinia 状态、路由守卫、axios 封装均按契约实现，`npm run build` 成功，E2E 脚本生成 11 张截图且主链路可跑通。

| 维度 | 评分 | 说明 |
|---|---|---|
| 页面完整性 | 优 | 9 个路由全部实现 |
| SchemaRender 引擎 | 优 | 支持 12 种 widgets 类型映射 |
| ECharts 组件 | 优 | 6 个图表组件封装完整 |
| 后端对接 | 优 | 已通过 `/api/ui/schema`、认证、统计、检测等接口联调 |
| 工程可构建 | 优 | `npm run build` 成功，仅 chunk 大小警告 |
| E2E 自测 | 良 | 11 张截图覆盖主链路，但脚本依赖本地 Chrome/Playwright 路径 |
| 移动端适配 | 中 | 当前为桌面端布局，M5 需补充响应式 |

---

## 二、关键文件核对

| 文件 | 状态 | 说明 |
|---|---|---|
| `package.json` | ✅ | Vue 3 + Vite + Element Plus + Pinia + Vue Router + axios + ECharts 栈正确 |
| `vite.config.js` | ✅ | 配置了 `/api` 和 `/models` 到 `http://127.0.0.1:8000` 的 dev proxy |
| `src/router/index.js` | ✅ | 9 条路由 + 登录守卫，未登录访问受保护页重定向到 /login |
| `src/utils/request.js` | ✅ | axios 拦截器自动加 Bearer token，统一错误处理 |
| `src/stores/user.js` | ✅ | Pinia 管理 token/用户信息，支持刷新恢复登录态 |
| `src/components/SchemaRender.vue` | ✅ | 12 种 widgets 映射，支持低代码 JSON 编辑 |
| `src/components/charts/*.vue` | ✅ | 饼图、折线图、柱状图、时间线、训练曲线面板均实现 |
| `src/pages/*.vue` | ✅ | 9 个页面均调用后端接口或静态资源 |
| `tests/e2e/manual_check.cjs` | ✅ | 覆盖注册/登录/各页面导航/图表渲染 |
| `dist/` 构建产物 | ✅ | `npm run build` 成功生成 |

---

## 三、联调验证

### 3.1 构建验证

```bash
cd frontend
npm run build
```

结果：**构建成功**，生成 `dist/` 目录，仅提示部分 chunk 大于 500kB（ECharts 全量引入导致，可接受）。

### 3.2 后端接口验证

已确认后端服务在 `http://127.0.0.1:8000` 运行：
- `GET /api/ui/schema` 返回完整 schema，nav/pages 结构与 W3 交付一致。
- 后端 CORS 已配置 `allow_origins=["*"]`，dev proxy 可正常转发。

### 3.3 E2E 截图

`frontend/tests/e2e/screenshots/` 下 11 张截图完整覆盖：
1. 未登录首页仪表盘
2. 未登录访问 history 重定向到 login
3. 注册过程
4. 登录成功
5. 登录后首页仪表盘
6. 检测工作台
7. 历史记录
8. 溯源验证
9. 个人中心
10. 低代码演示
11. 训练曲线看板

---

## 四、发现的问题

### 问题 1：history 页图表由前端补丁实现，未完全由 SchemaRender 驱动

**现象**：`src/pages/HistoryView.vue` 中直接硬编码了 `GradePieChart` 和 `ConfidenceLineChart`，而非通过 `SchemaRender` 根据 `ui_schema.json` 的 `history.widgets` 渲染。后端 `history.widgets` 仅含 `data-table`。

**影响**：
- 当前功能正常，不影响答辩演示。
- 长期来看，SchemaRender 的"配置驱动"理念在 history 页未彻底落地。

**建议**：
- **方案 A（推荐）**：保留当前前端补丁，同时让 W3 在 `src/backend/ui_schema.json` 的 `history.widgets` 中补充 `pie-chart` 与 `confidence-line-chart` 配置；W4 后续移除硬编码，改由 `SchemaRender` 渲染。
- **方案 B**：维持现状，将前端补丁作为永久方案，文档中说明 history 页为"混合渲染"。

### 问题 2：E2E 脚本依赖硬编码的本地 Playwright 路径

**现象**：`tests/e2e/manual_check.cjs` 第 1 行：
```js
const { chromium } = require('C:/nodejs/node_global/node_modules/@playwright/cli/node_modules/playwright-core')
```

**影响**：
- 换一台机器或答辩环境如果没有完全相同的 Node/Playwright 路径，脚本会失败。
- W5 复测时可能需要手动修改路径。

**建议**：
- 将 Playwright 作为 devDependency 安装到 `frontend/package.json` 中，使用 `require('playwright-core')` 或 `require('@playwright/test')`。
- 或提供不依赖 Playwright 的纯手动自测清单。

### 问题 3：生产环境 baseURL 为空

**现象**：`src/utils/request.js` 中 `baseURL: ''`，依赖 Vite dev proxy；生产部署时若前后端不同域，请求会 404。

**影响**：
- 当前开发/答辩本地演示无影响。
- 若部署到服务器需额外配置 `VITE_API_BASE_URL` 环境变量。

**建议**：
- 使用 `import.meta.env.VITE_API_BASE_URL || ''` 作为 baseURL，并在 `.env.development` 和 `.env.production` 中分别配置。

### 问题 4：训练曲线看板使用静态图片而非 ECharts 动态曲线

**现象**：`/training` 页展示 `/models/training_curves.png`（W2 生成的静态图）和 `/models/test_metrics.json`，未用 ECharts 从 CSV/JSON 动态绘制 loss/mAP 曲线。

**影响**：
- 答辩演示足够（有真实 W2 产物）。
- 若要体现"数据可视化深度"，动态 ECharts 曲线更佳。

**建议**：
- M5 可选任务：让 W2 提供 `results.csv` 解析接口或 CSV→JSON 转换，W4 用 ECharts 绘制 loss/mAP 曲线。

### 问题 5：chunk 大小警告

**现象**：`npm run build` 提示 `ConfidenceLineChart` 和 `index` chunk 大于 500kB。

**原因**：ECharts 全量引入（`import * as echarts from 'echarts'`）。

**建议**：
- M5 优化：改用 ECharts 按需引入（`echarts/lib/chart/line` 等），或配置 `manualChunks`。
- 当前不影响演示。

---

## 五、待决策项建议

W4 汇报中提出 4 个待决策项，建议批复如下：

| 编号 | 事项 | 建议方案 | 批复人 |
|---|---|---|---|
| D1 | history 页图表是否由后端 schema 提供？ | 保留前端补丁，同步让 W3 在 `ui_schema.json` 补全 `history.widgets`，M5 移除硬编码 | 组长 |
| D2 | training 页是否接入真实训练曲线？ | 当前已接入 W2 真实产物；M5 可选升级为 ECharts 动态曲线 | 组长 |
| D3 | M5 是否优先做移动端响应式适配？ | **否**，M5 优先完成端到端联调、缺陷修复、构建部署 | 组长/PM |
| D4 | 是否将前端同步到 GitHub 仓库？ | **是**，M4 结束后统一 push 整个项目到 GitHub，便于答辩部署与版本管理 | 组长/PM |

---

## 六、验收建议

### 6.1 必须通过项
- [x] 9 个页面全部可访问
- [x] `npm run build` 成功
- [x] 注册/登录/登出主链路可跑通
- [x] ECharts 图表可渲染
- [x] SchemaRender 可按 `/api/ui/schema` 渲染页面

### 6.2 建议 M5 处理项
- [ ] history 页彻底改为 SchemaRender 驱动
- [ ] E2E 脚本改为依赖项目内 Playwright
- [ ] 配置生产环境 `VITE_API_BASE_URL`
- [ ] 训练曲线看板可选升级为 ECharts 动态曲线
- [ ] 移动端响应式适配
- [ ] 项目推送到 GitHub 仓库

---

## 七、结论

**W4 M4 里程碑审查通过。** 前端工程完整、可构建、可运行，已与 W3 后端完成基础联调，主链路可演示。发现的 5 个问题均为非阻塞项，可在 M5 阶段逐步优化。

**建议组长批复 M4 通过，W4 进入 M5 阶段。**
