# W4 M5 里程碑汇报：端到端联调、缺陷修复与构建部署

**汇报人：** W4（前端）  
**汇报对象：** 组长（请批复） / 项目经理（PM，请转交）  
**汇报时间：** 2026-09-06  
**里程碑：** M5 — 端到端联调、缺陷修复、构建部署与项目同步  

---

## 1. 目标回顾

在 M4 已实现 9 个页面、SchemaRender 引擎、ECharts 组件与基础联调的前提下，M5 聚焦：
1. 端到端主链路跑通：注册 → 登录 → 上传茶叶图片 → 后端推理 → 前端展示结果与 trace_id → 历史记录 → 溯源验证。
2. 修复 M4 审查问题：history 页 Schema 驱动化、E2E 脚本可移植、生产环境 baseURL 配置、chunk 优化。
3. 构建与部署准备：验证 `npm run build` 产物，配置生产环境变量。
4. 项目同步到 GitHub。

---

## 2. 已完成工作清单

| 编号 | 任务 | 状态 | 交付物 |
|------|------|------|--------|
| M5-W4-01 | history 页 Schema 驱动化 | ✅ 完成 | `src/backend/ui_schema.json` 补全图表 widgets，`HistoryView.vue` 移除硬编码 |
| M5-W4-02 | detect 端到端联调 | ✅ 完成 | 前端可展示 class_name、confidence、bbox、trace_id |
| M5-W4-03 | trace 端到端联调 | ✅ 完成 | 用真实 trace_id 查询，哈希链时间线渲染通过 |
| M5-W4-04 | 生产环境 baseURL 配置 | ✅ 完成 | `request.js` 读取 `VITE_API_BASE_URL`，新增 `.env.development`/`.env.production` |
| M5-W4-05 | 项目同步到 GitHub | ✅ 完成 | 已推送至 https://github.com/c8shakei/tea-yulu-grading-platform |
| M5-W4-06 | E2E 脚本可移植化 | ✅ 完成 | `package.json` 增加 `test:e2e` 脚本，移除硬编码 Playwright 路径 |
| M5-W4-07 | ECharts 按需引入优化 | ✅ 完成 | 新增 `src/utils/echarts.js` 统一注册必要组件，减少全量引入 |

---

## 3. 关键修复记录

### 3.1 history 页改为 SchemaRender 驱动

**问题**：`HistoryView.vue` 直接硬编码 `GradePieChart` 与 `ConfidenceLineChart`，与 SchemaRender 的"配置驱动"理念不一致。

**修复**：
- 在 `src/backend/ui_schema.json` 的 `history.widgets` 中补充：
  - `pie-chart`（`metric=grade_distribution`，`title=个人等级分布`）
  - `line-chart`（`metric=confidence_trend`，`title=平均置信度趋势`）
- 重写 `HistoryView.vue`：仅注入 schema、加载数据、通过 `SchemaRender` 渲染全部 widgets。

### 3.2 修复检测页上传回调 Bug

**问题**：`SchemaRender.vue` 中 `handleUpload({ file })` 错误地从 Element Plus `on-change` 回调中解构不存在的 `file` 属性，导致 `file.raw` 读取失败，前端无法触发 `POST /api/detect`。

**修复**：改为 `handleUpload(uploadFile)`，使用 `uploadFile?.raw`。

**验证**：E2E 中 `detect trace_id` 从 `null` 变为真实 32 位哈希，前端正确展示：
- 等级：一级
- 置信度：92.10%
- 边界框：\[0.003665..., 0, 1, 0.997714...\]
- 溯源 ID：0344eb1f1153434988877bc5911093a5

### 3.3 E2E 脚本可移植化

**问题**：`tests/e2e/manual_check.cjs` 第 1 行硬编码 `C:/nodejs/...` 的 Playwright 路径，换环境即失效。

**修复**：
- 安装 `@playwright/test` 为 devDependency。
- 脚本改为 `require('playwright-core')`。
- `package.json` 新增 `"test:e2e": "node tests/e2e/manual_check.cjs"`。
- 脚本使用 `page.waitForEvent('filechooser')` + `fileChooser.setFiles()` 完成真实上传。

### 3.4 生产环境 baseURL 配置

**修复**：
- `src/utils/request.js`：`baseURL: import.meta.env.VITE_API_BASE_URL || ''`
- `frontend/.env.development`：保留空值，配合 Vite proxy `/api` → `:8000`
- `frontend/.env.production`：提供配置注释，可设为实际后端地址或保持空值（由 Nginx 反向代理）

### 3.5 ECharts 按需引入

**修复**：
- 新增 `src/utils/echarts.js`，仅注册 `PieChart`、`LineChart`、`BarChart`、`CanvasRenderer`、`TitleComponent`、`TooltipComponent`、`LegendComponent`、`GridComponent` 等必要模块。
- `GradePieChart.vue`、`TrendLineChart.vue`、`ConfidenceLineChart.vue` 统一从该工具引入。

**效果**：
- `ConfidenceLineChart` chunk 从 1,038.48 kB 降至 580.31 kB（gzip 后约 197 kB）。
- `index` chunk 从 1,259.38 kB 降至 6.41 kB，Element Plus 单独拆分为 1,088.33 kB。

---

## 4. 端到端联调记录

### 4.1 测试链路

1. 未登录访问 `/history` → 重定向 `/login`
2. 注册新用户 `w4test_<timestamp>`
3. 登录后跳转首页
4. `/detect` 上传 `test_tea.jpg`
5. 后端推理返回：`{"class_name":"一级","confidence":0.9209...,"trace_id":"0344eb1f..."}`
6. 前端展示检测结果
7. `/trace` 输入 trace_id，查询返回哈希链并渲染
8. 访问 `/history`、`/profile`、`/lowcode`、`/training` 正常

### 4.2 关键截图

E2E 截图保存在 `frontend/tests/e2e/screenshots/`：
- `06_detect.png`：展示真实检测结果
- `08_trace.png`：展示哈希链验证通过
- `07_history.png`：展示 SchemaRender 渲染的饼图、折线图、数据表

---

## 5. 构建验证

```bash
cd frontend
npm run build
```

结果：**构建成功**，生成 `dist/` 目录，仅 Element Plus 与 ECharts 组件 chunk 大于 500 kB 警告（不影响运行）。

---

## 6. GitHub 同步状态

- 本地已初始化 git 仓库，分支 `main`。
- 已提交 2 条 commit：
  - `54ea8eb` W4 M5: frontend optimization, schema-driven history, env config, e2e portability
  - `eb83087` W4 M5: fix upload callback, e2e full detect/trace verification
- `.gitignore` 已排除 `*.pt`、前端 `node_modules/`、`dist/`、截图缓存等。

**已完成**：本地 `main` 分支已推送至 `origin/main`。  
- 仓库地址：https://github.com/c8shakei/tea-yulu-grading-platform  
- 由项目经理助理使用 `gh repo create --public --source=. --remote=origin --push` 完成同步。  
- `.gitignore` 已排除 `.pt`、`node_modules/`、`dist/`、`datasets/` 等大文件。

---

## 7. 阻塞风险

| 风险 | 状态 | 说明 |
|------|------|------|
| 训练曲线动态化 | ⏸️ 未开始 | 依赖 W2 提供 CSV/JSON 训练日志接口，当前为静态 PNG 方案 |
| 移动端响应式适配 | ⏸️ 未开始 | P2 可选，主链路稳定后再做 |

---

## 8. 待决策项

| 编号 | 事项 | 建议 | 决策人 |
|------|------|------|--------|
| D1 | 训练曲线动态化 | 等待 W2 接口就绪后再做 | 组长 |
| D2 | 是否开启移动端适配 | 建议在答辩前一周补做 | 组长/PM |

---

## 9. 审批申请

W4 已完成 M5 核心目标（端到端联调、M4 审查问题修复、构建验证、GitHub 仓库初始化）。

现申请组长批复 M5 通过。

- **申请人：** W4
- **申请时间：** 2026-09-06
- **批复人：** 组长（请批复）
